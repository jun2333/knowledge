import{_ as e,o as s,c as o,a4 as a}from"./chunks/framework.BBs01uty.js";const h=JSON.parse('{"title":"Java 实战收获记录","description":"","frontmatter":{},"headers":[],"relativePath":"java-practice/harvest.md","filePath":"java-practice/harvest.md"}'),i={name:"java-practice/harvest.md"};function n(r,t,l,d,c,p){return s(),o("div",null,[...t[0]||(t[0]=[a(`<h1 id="java-实战收获记录" tabindex="-1">Java 实战收获记录 <a class="header-anchor" href="#java-实战收获记录" aria-label="Permalink to &quot;Java 实战收获记录&quot;">​</a></h1><blockquote><p>记录在 mall 实战过程中沉淀的收获与踩坑，按主题持续追加。 与阶段文档的区分：阶段文档讲&quot;怎么做&quot;，这里记&quot;实际遇到了什么、学到了什么&quot;。</p></blockquote><h2 id="_2026-08-31-首篇" tabindex="-1">2026-08-31（首篇） <a class="header-anchor" href="#_2026-08-31-首篇" aria-label="Permalink to &quot;2026-08-31（首篇）&quot;">​</a></h2><h3 id="一、工程结构与依赖-maven-多模块" tabindex="-1">一、工程结构与依赖（Maven 多模块） <a class="header-anchor" href="#一、工程结构与依赖-maven-多模块" aria-label="Permalink to &quot;一、工程结构与依赖（Maven 多模块）&quot;">​</a></h3><ul><li><strong>Maven 多模块 = 后端 monorepo</strong>：一个仓库、多个可独立构建的模块，依赖方向单向（底座 ← 业务）。前端对应 pnpm workspace。</li><li><strong>模块划分规范</strong>：横切分层（mall-common / mall-mbg / mall-security 底座）+ 纵切业务（mall-admin / mall-portal 两个入口）。判断边界四问：依赖单向吗？通用下沉了吗？能独立编译吗？有循环依赖吗？</li><li><strong>pom.xml 三块</strong>：<code>&lt;properties&gt;</code> 统一定版本 → <code>&lt;dependencies&gt;</code> + <code>&lt;dependencyManagement&gt;</code> 声明依赖 → <code>&lt;build&gt;</code> 配置构建。</li><li><strong><code>dependencies</code> vs <code>dependencyManagement</code></strong>：前者&quot;我要用&quot;（子模块自动继承），后者&quot;用的话必须这个版本&quot;（版本清单，子模块要自己声明才拥有）。多模块版本统一靠 DM + properties。</li><li><strong>坐标</strong>：<code>groupId</code>（组织，反向域名）= npm 的 scope，<code>artifactId</code>（模块名）= npm 包名，<code>version</code> = 版本。三者唯一标识一个 jar。</li></ul><h3 id="二、java-语言细节-澄清与踩坑" tabindex="-1">二、Java 语言细节（澄清与踩坑） <a class="header-anchor" href="#二、java-语言细节-澄清与踩坑" aria-label="Permalink to &quot;二、Java 语言细节（澄清与踩坑）&quot;">​</a></h3><ul><li><strong>enum 本质是&quot;类&quot;</strong>：可带字段/构造器/方法/接口实现，隐式继承 <code>java.lang.Enum</code>，<strong>实例在声明时定死、不能 new、天然单例</strong>。适合&quot;固定数量 + 每个常量带属性&quot;（如 ResultCode）。</li><li><strong>equals</strong>：所有非 null 对象都有（继承 Object，默认比地址），String/包装类重写为<strong>内容比较</strong>；<strong>null 调用 equals 直接 NPE</strong>。最省心的写法是 <code>Objects.equals(a, b)</code>（两边都防 null）。</li><li><strong>装箱/拆箱</strong>：<code>int ↔ Integer</code> 自动转换；<strong><code>Integer</code> 的 <code>==</code> 陷阱</strong>（-128~127 有缓存，超出范围 <code>==</code> 比地址不同对象 → false，要用 <code>equals</code>）。</li><li><strong>equals 与 hashCode 契约</strong>：equals 相等 → hashCode 必相等；重写 equals 必须重写 hashCode（HashMap 依赖）。record / Lombok @Data 自动处理。</li></ul><h3 id="三、spring-注入与实例化" tabindex="-1">三、Spring 注入与实例化 <a class="header-anchor" href="#三、spring-注入与实例化" aria-label="Permalink to &quot;三、Spring 注入与实例化&quot;">​</a></h3><ul><li><strong><code>@RequiredArgsConstructor</code> + <code>final</code> 字段 = 构造器注入</strong>：Lombok 编译期生成构造器（只含 final/@NonNull 字段），Spring 看到唯一构造器自动注入，不需要 @Autowired。</li><li><strong><code>@Autowired</code> 字段注入已过时</strong>：不可测试（私有字段 new 不进去）、隐藏依赖（构造器一眼看清依赖）、循环依赖静默（字段注入启动不报错、运行时才炸）。现代推荐<strong>构造器注入</strong>（final + @RequiredArgsConstructor）。</li><li><strong>Spring 4.3+ 单构造器自动注入</strong>：只有一个构造器时连 @Autowired 都不用写，Spring 自动用该构造器注入——所以构造器注入代码里通常看不到 @Autowired 注解。</li><li><strong>IoC/DI</strong>：对象的创建权交给框架——启动时扫描 @Service/@Bean 实例化并注入，业务代码从不 new 业务 Bean。这解释了&quot;为什么构造器注入能成立&quot;。</li></ul><h3 id="四、mall-实战改造-真实动手" tabindex="-1">四、mall 实战改造（真实动手） <a class="header-anchor" href="#四、mall-实战改造-真实动手" aria-label="Permalink to &quot;四、mall 实战改造（真实动手）&quot;">​</a></h3><ul><li><strong>循环依赖重构</strong>：UmsAdminService ↔ CacheService 互引，原用 <code>SpringUtil.getBean</code> 绕环。改为构造器注入 + CacheService 去掉对 AdminService 的依赖（直接注入 Mapper），<strong>依赖单向化</strong>。</li><li><strong>HTTP 状态码语义化</strong>：<code>ResponseBodyAdvice</code> 按 body.code 映射 HTTP 状态码 + 401/403 的 EntryPoint/DeniedHandler 手动 <code>setStatus</code>。常规做法其实是源头设置（ResponseEntity），ResponseBodyAdvice 是&quot;老项目不改所有 Controller&quot;的过渡方案。</li><li><strong>业务码体系</strong>：<code>ResultCode</code> 改 <code>2xxxx/4xxxx/5xxxx</code> 分段，<strong>枚举自带 <code>HttpStatus</code></strong>（<code>httpStatusOf(code)</code> 查码 + 兜底 500），映射单一来源。</li><li><strong>token 失效方案</strong>：JWT 无状态 → 改密码后旧 token 依然有效（validateToken 只验签名+过期+用户名）。方案：<strong>token 里带密码 hash 指纹</strong>，改密码后数据库密码变 → 校验失败。登出仍要黑名单/时间戳方案。</li></ul><h3 id="五、排查经验-最值钱的沉淀" tabindex="-1">五、排查经验（最值钱的沉淀） <a class="header-anchor" href="#五、排查经验-最值钱的沉淀" aria-label="Permalink to &quot;五、排查经验（最值钱的沉淀）&quot;">​</a></h3><ul><li><strong>404 → /error → 401 链路</strong>：看似&quot;被安全拦截&quot;，实则是<strong>路径 404 后 Spring 转发 <code>/error</code>，而 <code>/error</code> 不在白名单</strong>。日志特征：<code>Secured 原路径</code> 后紧跟 <code>Securing /error</code>。Druid 监控页 401 的根因就是这个（StatViewServlet 未注册 → 404）。</li><li><strong>MvcRequestMatcher vs PathPatternRequestMatcher</strong>：<code>requestMatchers(String)</code> 默认 MvcRequestMatcher，其行为受 <code>spring.mvc.pathmatch.matching-strategy</code> 影响。排查时先怀疑&quot;配置是否真的生效&quot;，用临时打印/调试日志确认，别猜。</li><li><strong>Druid 监控</strong>：两层都要开——<code>stat-view-servlet.enabled: true</code>（监控页）+ <code>filters: stat</code>（SQL 统计）。SQL 显示为 <code>?</code> 是<strong>参数化查询</strong>（防注入 + 预编译），参数值看 MyBatis 日志的 <code>Parameters:</code> 行。</li><li><strong>PageHelper 分页 = 两条 SQL</strong>：一条列表（LIMIT）+ 一条 <code>count(0)</code>（同条件算总数），count 结果进 <code>Page.total</code> → <code>CommonPage.total/totalPage</code>。</li><li><strong>环境</strong>：命令行 <code>mvn</code> 用 JDK 17 靠 <code>JAVA_HOME</code> 环境变量（<code>~/.zshrc</code> 里配），IDEA 用 Project SDK（不读 shell）。</li></ul><h3 id="六、验证过的调试手段" tabindex="-1">六、验证过的调试手段 <a class="header-anchor" href="#六、验证过的调试手段" aria-label="Permalink to &quot;六、验证过的调试手段&quot;">​</a></h3><ul><li>看真实 SQL：<code>logging.level.com.macro.mall.mapper: debug</code>（Preparing + Parameters）+ Druid SQL 监控页（执行统计）。</li><li>定位安全拦截：<code>org.springframework.security: debug</code> 日志看过滤器链匹配过程（<code>Securing/Secured</code>）。</li><li>确认 class 是否最新：<code>javap</code> 反编译 + 对比进程启动时间和 class 更新时间（排查&quot;改了没生效&quot;）。</li></ul><h3 id="七、数据访问与更新-事务-全量vs差异-动态查询" tabindex="-1">七、数据访问与更新（事务 / 全量vs差异 / 动态查询） <a class="header-anchor" href="#七、数据访问与更新-事务-全量vs差异-动态查询" aria-label="Permalink to &quot;七、数据访问与更新（事务 / 全量vs差异 / 动态查询）&quot;">​</a></h3><p><strong>事务注解位置（CGLIB 代理坑）</strong></p><ul><li><code>@Transactional</code> 放<strong>接口</strong>上在 Spring Boot 3（默认 CGLIB 代理，基于类）下<strong>不生效</strong>——注解不被继承，CGLIB 只认类上的注解。</li><li>正确姿势：放<strong>实现类方法</strong>上 + <code>rollbackFor = Exception.class</code>（默认只回滚 RuntimeException，受检异常也要回滚就加它）。</li><li><strong>验证事务生效</strong>：<code>logging.level.org.springframework.transaction.interceptor: trace</code> → 日志见 <code>Creating new transaction</code> / <code>Committing transaction</code> / <code>Rolling back transaction</code>。<strong>没有 Creating = 事务没被代理</strong>。</li></ul><p><strong>全量更新 vs 差异更新（先删后插）</strong></p><ul><li>mall 子表更新 = <code>deleteByExample</code>（按 productId 全删）+ <code>relateAndInsertList</code>（全量重插），简单一致。</li><li><strong>纯配置子表</strong>（会员价/阶梯价/满减/属性）：无状态、无外部引用 → 可删了重插。</li><li><strong>有状态子表</strong>（SKU）：有库存运行状态（删了重插会覆盖真实库存）+ 被订单/购物车引用 id（删了重插断关联）→ <strong>必须 diff</strong>（<code>handleUpdateSkuStockList</code>：新增 insert / 删除 delete / 变更 update，按 id 有无判断）。</li><li>判断标准：<strong>数据是静态配置还是运行状态 + 有没有外部引用</strong>。</li><li>多子表 SQL 必须<strong>串行</strong>（同一事务同一连接），并行 = 多连接 = 失去原子性；并行只留给&quot;独立 + 耗时 + 不需要事务&quot;的远程调用。</li></ul><p><strong>Example 动态查询两层 if</strong></p><ul><li><strong>Java 层 if</strong>（null 守卫）：值非 null 才 <code>andXxxEqualTo(value)</code>，决定&quot;条件集合有什么&quot;。</li><li><strong>Example 条件对象</strong>：只记&quot;有没有这个条件&quot;（condition + value），<strong>不关心值是否为 null</strong>。</li><li><strong>XML <code>Example_Where_Clause</code></strong>：翻译器（MBG 生成），<code>foreach</code> 遍历条件组 + <code>if test=&quot;criteria.valid&quot;</code> + <code>choose</code> 按类型拼 SQL（noValue→<code>is null</code>类 / singleValue→<code>= ?</code> / betweenValue→<code>between</code> / listValue→<code>in (...)</code>）。</li><li><strong>坑</strong>：Java 无脑丢 null → Example 照收 → XML 照拼 → SQL 出现 <code>= null</code> → 查不出（<code>=</code> 对 NULL 永远不成立，要用 <code>IS NULL</code>）。所以判空必须在 Java 层。</li><li><strong>判断模式</strong>：Java 传 Example 对象 = Example 模式（条件判断在 Java）；Java 传普通参数 + XML <code>&lt;if test&gt;</code> = XML 模式。</li></ul><p><strong>SKU 编码生成</strong>：<code>StringBuilder</code> 高效拼接 + <code>String.format(&quot;%04d&quot;, id)</code> 补零到指定宽度（前端等价 <code>String(id).padStart(4,&#39;0&#39;)</code>），如 <code>日期 + 商品id(4位) + 序号(3位)</code> → <code>202608310012003</code>。</p><p><strong>前端配合后端状态码改造</strong>（mall-admin-web）：</p><ul><li>后端改语义化 HTTP 状态码后，axios 的 401/403 走 <strong>error 分支</strong>（不是 response 分支）→ 必须补 error 分支：401 → 登出弹窗，其他 → 从 <code>error.response.data.message</code> 取提示。</li><li>业务码判断同步改：成功 <code>200</code> → <code>20000</code>，未登录 <code>401</code> → <code>40100</code>。</li></ul><h3 id="八、aop-实战-通知类型与切入点-weblogaspect" tabindex="-1">八、AOP 实战：通知类型与切入点（WebLogAspect） <a class="header-anchor" href="#八、aop-实战-通知类型与切入点-weblogaspect" aria-label="Permalink to &quot;八、AOP 实战：通知类型与切入点（WebLogAspect）&quot;">​</a></h3><p><strong>核心直觉：AOP 通知 = 钩子函数</strong>（框架在特定时机回调你定义的逻辑）。mall 的 <code>WebLogAspect</code>（mall-common）是完整演示。</p><p><strong>通知类型（5 种钩子）</strong>：</p><table tabindex="0"><thead><tr><th>通知</th><th>时机</th><th>能拿到</th><th>类比前端</th></tr></thead><tbody><tr><td><code>@Before</code></td><td>目标方法<strong>执行前</strong></td><td>JoinPoint（方法签名/参数）</td><td>中间件 <code>next()</code> 前</td></tr><tr><td><code>@After</code></td><td>执行后（成功/异常都触发）</td><td>—</td><td><code>finally</code></td></tr><tr><td><code>@AfterReturning</code></td><td><strong>正常返回后</strong>（异常不触发）</td><td><strong>返回值</strong>（<code>returning=&quot;ret&quot;</code>）</td><td><code>res</code> 回调</td></tr><tr><td><code>@AfterThrowing</code></td><td>抛异常后</td><td>异常对象</td><td><code>catch</code></td></tr><tr><td><code>@Around</code></td><td><strong>前后都包</strong>（最灵活）</td><td>全都有，<code>proceed()</code> 控制</td><td>中间件包 <code>next()</code></td></tr></tbody></table><p><strong><code>@Around</code> 是最全的钩子</strong>：proceed() 前 = before，proceed() 后 = after/afterReturning，还能 catch 异常。mall 用 @Around 一把梭，@Before/@AfterReturning 留空占位。</p><p><strong>切入点表达式拆解</strong>（<code>execution(...)</code>）：</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Pointcut</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;execution(public * com.macro.mall.controller.*.*(..))</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          || execution(public * com.macro.mall.*.controller.*.*(..))&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> webLog</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() { }   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 空方法 = 切入点的&quot;命名引用&quot;，供通知引用</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>execution( 访问修饰符 返回类型 包.类.方法(参数) )</span></span>
<span class="line"><span>  public  *        com.macro.mall.controller . * . * (..)</span></span>
<span class="line"><span>  只切public  任意返回    指定包           类任意 方法任意 参数任意</span></span></code></pre></div><ul><li><code>||</code> = 或（再匹配一级子包下的 controller 包，如 portal/admin 的 controller）</li><li><code>..</code> = 任意参数（0 个或多个）</li></ul><p><strong>WebLogAspect 实战要点</strong>：</p><ul><li><code>@Around</code> 包住方法：proceed() 前拿 <code>HttpServletRequest</code> 记开始时间 → proceed() 执行 → 后组装 <code>WebLog</code>（url/method/参数/耗时/结果）→ 通过 Logstash 打进 Elasticsearch</li><li><strong>等于前端&quot;请求日志中间件&quot;</strong>：不用每个接口手写日志，切一刀统一记录（横切关注点）</li></ul><p><strong>钩子函数的心智模型</strong>：AOP 通知 = 框架留的钩子位（before/after/around），你填逻辑，框架到点调你。<code>@Around</code> 的 <code>proceed()</code> 是&quot;把控制权交给真实方法&quot;的分水岭——<strong>单切面时是包装（装饰器），多切面叠起来才成洋葱</strong>。</p><h3 id="九、注解-装饰器-高阶函数-概念澄清" tabindex="-1">九、注解 / 装饰器 / 高阶函数（概念澄清） <a class="header-anchor" href="#九、注解-装饰器-高阶函数-概念澄清" aria-label="Permalink to &quot;九、注解 / 装饰器 / 高阶函数（概念澄清）&quot;">​</a></h3><p><strong>HOF（高阶函数）严格定义</strong>：<strong>接收函数作为参数，&quot;或&quot;返回函数作为结果的函数</strong>——是&quot;或&quot;（either/or），<strong>不是&quot;且&quot;</strong>（不需要同时满足）。</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">arr.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fn)                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 接收函数（不返回函数）→ 是 HOF</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> f</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {}    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 返回函数 → 是 HOF</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withLog</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fn)                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 接收且返回 → 是 HOF（也是装饰器）</span></span></code></pre></div><ul><li><strong>HOF 是大集合</strong>：map/filter/reduce、<code>addEventListener</code>、<code>setTimeout</code>、<code>Promise.then</code> 都是 HOF（接收回调就算）</li><li><strong>装饰器是 HOF 的子集</strong>：函数式装饰器（<code>withLog(fn)</code>）是 HOF 中&quot;包装增强&quot;的那一类；map 是 HOF 但不是装饰器（只消费回调，不包装增强原函数）</li></ul><p><strong>注解 ≠ 装饰器</strong>：</p><ul><li>注解 = <strong>元数据</strong>（数据标记），本身无行为；装饰器 = <strong>行为</strong>（包装增强）</li><li>但&quot;注解 + 框架&quot;能触发装饰行为：Spring 读 <code>@Transactional</code> → 运行时生成代理装饰 bean</li><li>所以&quot;注解像装饰器&quot;的感觉来自<strong>框架</strong>，不是注解本身</li></ul><p><strong>注解处理的两条路</strong>（看 <code>@Retention</code>）：</p><table tabindex="0"><thead><tr><th>时机</th><th>谁做</th><th>例子</th><th>Retention</th></tr></thead><tbody><tr><td><strong>编译期</strong></td><td>javac 注解处理器</td><td>Lombok（@Data 生成 getter/setter）</td><td><code>SOURCE</code>（编译后即弃）</td></tr><tr><td><strong>运行时</strong></td><td>启动时反射 + 动态代理</td><td>Spring（@Service/@Transactional → 代理）</td><td><code>RUNTIME</code>（字节码保留，反射可读）</td></tr></tbody></table><p><strong>三者关系一句话</strong>：注解 = &quot;声明&quot;（让框架做），装饰器 = &quot;包装&quot;（自己做），高阶函数 = &quot;函数的能力&quot;（接收/返回函数）；注解经框架触发才表现成装饰器，装饰器函数式实现时是高阶函数的一种，高阶函数范围最大。</p><h3 id="十、日志配置与-elk-链路-logback-→-logstash-→-es" tabindex="-1">十、日志配置与 ELK 链路（logback → Logstash → ES） <a class="header-anchor" href="#十、日志配置与-elk-链路-logback-→-logstash-→-es" aria-label="Permalink to &quot;十、日志配置与 ELK 链路（logback → Logstash → ES）&quot;">​</a></h3><p><strong>日志链路三件套</strong>（代码里打日志到最终落地的完整路径）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>log.info(...) / @Slf4j 的 log</span></span>
<span class="line"><span>  → SLF4J（门面/接口）</span></span>
<span class="line"><span>  → logback（实现，Spring Boot 默认）</span></span>
<span class="line"><span>  → logback-spring.xml 配置的 appender（决定&quot;输出到哪&quot;）</span></span>
<span class="line"><span>      ├─ ConsoleAppender            → 控制台</span></span>
<span class="line"><span>      ├─ RollingFileAppender        → 文件（滚动，\`class\` 属性定类型 + \`%d\`/\`%i\` 定文件名）</span></span>
<span class="line"><span>      └─ LogstashTcpSocketAppender  → 网络 TCP 发到 Logstash</span></span></code></pre></div><p><strong>logback 配置核心概念</strong>：</p><table tabindex="0"><thead><tr><th>概念</th><th>作用</th></tr></thead><tbody><tr><td>appender</td><td>输出目标（class 属性定类型：控制台/文件/网络端口）</td></tr><tr><td>logger / root</td><td>级别配置（<code>name</code> 是包名前缀，按来源包分级）</td></tr><tr><td>pattern</td><td>格式（<code>%d</code>时间 <code>%level</code>级别 <code>%logger</code>类 <code>%message</code>消息）</td></tr><tr><td>filter</td><td>appender 内再过滤（ThresholdFilter ≥X / LevelFilter 只 X）</td></tr><tr><td>rollingPolicy</td><td>文件滚动（<code>%d</code> 按天 + <code>%i</code> 大小切分序号，留 30 天）</td></tr></tbody></table><p><strong>三个易混点</strong>：</p><ul><li><strong>决定&quot;输出到哪&quot;的是 appender 的 <code>class</code></strong>，具体位置（路径/端口）由内部 <code>&lt;fileNamePattern&gt;</code>/<code>&lt;destination&gt;</code> 定；<code>&lt;appender-ref&gt;</code> 只是&quot;选出口&quot;</li><li><strong>additivity（默认 true）</strong>：logger 日志除了发自己绑的 appender，还向上叠加发给 root 的——想&quot;只进自己分类管道&quot;要加 <code>additivity=&quot;false&quot;</code></li><li><strong>logback <code>&lt;if&gt;</code> 标准写法</strong>：条件内联 <code>&lt;if condition=&#39;property(&quot;X&quot;).equals(&quot;true&quot;)&#39;&gt;</code>，需要 Janino 支持；非标准写法（<code>&lt;condition&gt;</code> 拆到 if 前）可能不生效</li></ul><p><strong>Logstash 收→发流程</strong>（应用只负责&quot;发&quot;，存哪由 Logstash 定）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>应用（logback，TCP 发 4560-4563 端口，JSON 格式）</span></span>
<span class="line"><span>  → Logstash 服务（document/elk/logstash.conf）</span></span>
<span class="line"><span>       input：监听 4560(debug)/4561(error)/4562(business)/4563(record)</span></span>
<span class="line"><span>       filter：解析 JSON、清理字段</span></span>
<span class="line"><span>       output：elasticsearch → localhost:9200，索引 mall-{type}-{日期}</span></span>
<span class="line"><span>  → Elasticsearch（存储，全文搜索）</span></span>
<span class="line"><span>  → Kibana（可视化查日志）</span></span></code></pre></div><p><strong>关键认知</strong>：</p><ul><li>应用项目里<strong>没有 ES 配置</strong>（只有 <code>logstash.host</code>）——ES 连接/索引在 <strong>Logstash 的配置文件</strong>（部署层）</li><li>TCP 日志的落脚点是 <strong>ES</strong>（不是文件系统，不是 MySQL）——Elasticsearch 是&quot;日志/搜索的搜索引擎存储&quot;</li><li>mall-search 的 ES 是<strong>另一回事</strong>（商品搜索），别和日志 ELK 混</li></ul><h2 id="_2026-09-02-第二篇" tabindex="-1">2026-09-02（第二篇） <a class="header-anchor" href="#_2026-09-02-第二篇" aria-label="Permalink to &quot;2026-09-02（第二篇）&quot;">​</a></h2><h3 id="十一、认证改造-authenticationmanager-登录交给框架" tabindex="-1">十一、认证改造：AuthenticationManager（登录交给框架） <a class="header-anchor" href="#十一、认证改造-authenticationmanager-登录交给框架" aria-label="Permalink to &quot;十一、认证改造：AuthenticationManager（登录交给框架）&quot;">​</a></h3><p>mall 登录从&quot;手动验证密码&quot;改为&quot;框架认证&quot;：</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 改前：手动 loadUserByUsername + passwordEncoder.matches + 手动 new 已认证对象</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 改后：交给框架</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Authentication authentication </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authenticationManager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">authenticate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> UsernamePasswordAuthenticationToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(username, password));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">UserDetails userDetails </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (UserDetails) authentication.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getPrincipal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">String token </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> jwtTokenUtil.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">generateToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(userDetails);</span></span></code></pre></div><p><strong>关键认知</strong>：</p><ul><li><strong>自动装配是&quot;按类型推断（byType）&quot;不是按名字</strong>：框架 <code>getBean(UserDetailsService.class)</code> 从容器找 Bean（MallSecurityConfig 提供，内部调 adminService），名字随便叫都能找到；UserDetailsService 是框架的 <strong>SPI 接口约定</strong>，你实现放进容器，框架自动接上</li><li><strong>AuthenticationManager 接口是框架的，但 Bean 要自己暴露</strong>（<code>config.getAuthenticationManager()</code>）</li><li><strong>DaoAuthenticationProvider 内置检查</strong>：查用户（UserDetailsService）+ 比对密码（PasswordEncoder）+ <code>isEnabled()</code>（禁用抛 DisabledException）、密码错抛 BadCredentialsException（用户不存在被框架隐藏成 BadCredentialsException 防枚举）</li></ul><h3 id="十二、循环依赖-原理与-objectprovider-断环" tabindex="-1">十二、循环依赖：原理与 ObjectProvider 断环 <a class="header-anchor" href="#十二、循环依赖-原理与-objectprovider-断环" aria-label="Permalink to &quot;十二、循环依赖：原理与 ObjectProvider 断环&quot;">​</a></h3><p><strong>环的形成</strong>（本次实战）：UmsAdminService 依赖 AuthenticationManager，而 AuthenticationManager 的创建链（SecurityConfig → JwtAuthenticationTokenFilter → UserDetailsService → UmsAdminService）又必须经过 UmsAdminService——A要B→C→D→A，谁都等不到谁（类似 JS 模块循环引用）。</p><p><strong>修复</strong>：<code>ObjectProvider&lt;AuthenticationManager&gt;</code> 懒获取——注入时不创建目标 Bean，login 用时才 <code>getObject()</code> 解析：</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ObjectProvider&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AuthenticationManager</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; authenticationManagerProvider;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// login: authenticationManagerProvider.getObject().authenticate(...)</span></span></code></pre></div><p><strong>经验套路</strong>：循环依赖 = 画&quot;谁需要谁&quot;的链找到绕回起点的依赖，把它改成懒获取（ObjectProvider/@Lazy）或拆开。别用 <code>allow-circular-references=true</code>（掩盖问题）。</p><h3 id="十三、异常分层与认证异常处理-最佳实践落地" tabindex="-1">十三、异常分层与认证异常处理（最佳实践落地） <a class="header-anchor" href="#十三、异常分层与认证异常处理-最佳实践落地" aria-label="Permalink to &quot;十三、异常分层与认证异常处理（最佳实践落地）&quot;">​</a></h3><p><strong>分布原则</strong>：异常<strong>往上抛、全局统一接、别到处 try-catch</strong>——Service 主动抛（知道为什么失败）、Controller 零判断（只写成功路径）、全局异常处理器统一转格式、技术异常（DB/IO）别吞。</p><p><strong>本次改造</strong>（登录异常）：</p><ul><li>Controller 删掉 <code>if (token == null)</code> 判断（原：返回值判断 = 职责越界 + 原因丢失）</li><li>Service catch 后<strong>抛带业务码的 ApiException</strong>（不再返回 null 丢原因）：</li></ul><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (DisabledException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ApiException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ResultCode.FORBIDDEN, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;账号已被禁用&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 40300/403</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (BadCredentialsException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ApiException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ResultCode.PARAM_ERROR, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;用户名或密码错误&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 40000/400</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li>全局异常处理器 <code>handle(ApiException)</code> 用 <code>failed(errorCode, e.getMessage())</code> 返回</li><li>ApiException 新增 <code>(IErrorCode, String message)</code> 构造器</li></ul><p><strong>分层约束</strong>：<code>mall-common</code> 是通用底座<strong>不能依赖 spring-security</strong>（DisabledException 在 spring-security-core，common 引不到）——安全异常要在依赖了 security 的 mall-admin 处理，再用自己的业务异常（ApiException）桥接给全局（mall-common 只处理通用异常）。</p><p><strong>缓存一致性小坑</strong>：改了 DB 里的缓存数据源（如 status）<strong>必须清 Redis</strong>（<code>redis-cli -n 0 flushdb</code> 或删对应 key），否则 <code>getAdminByUsername</code> 先查缓存拿到旧值——&quot;改库 + 清缓存&quot;是配套动作。</p><h2 id="_2026-09-03-第三篇" tabindex="-1">2026-09-03（第三篇） <a class="header-anchor" href="#_2026-09-03-第三篇" aria-label="Permalink to &quot;2026-09-03（第三篇）&quot;">​</a></h2><h3 id="十四、并发锁-从-背-api-到-用定义分界做决策-今日主线" tabindex="-1">十四、并发锁：从&quot;背 API&quot;到&quot;用定义分界做决策&quot;（今日主线） <a class="header-anchor" href="#十四、并发锁-从-背-api-到-用定义分界做决策-今日主线" aria-label="Permalink to &quot;十四、并发锁：从&quot;背 API&quot;到&quot;用定义分界做决策&quot;（今日主线）&quot;">​</a></h3><p><strong>乐观/悲观阵营模型</strong>（自己推出来的）：分界 = <strong>加不加锁、阻不阻塞</strong>，不是&quot;用不用 version&quot;：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>悲观阵营：FOR UPDATE（加锁阻塞）</span></span>
<span class="line"><span>乐观阵营：不加锁假设成功、失败检测返回 0</span></span>
<span class="line"><span>  ├─ 条件型（原子 SQL stock=stock-1）：不读快照，一次闭环</span></span>
<span class="line"><span>  └─ 版本型（version/时间戳）：先读快照，冲突重试</span></span></code></pre></div><ul><li>按此定义<strong>原子 SQL 也是乐观的</strong>（教材把版本型特指叫乐观锁 → 口径混乱的根源）</li><li>乐观锁本质 = &quot;<strong>WHERE 带旧值</strong>&quot;：version / 时间戳 / CAS 条件（<code>stock&gt;=1</code>）/ 状态机校验（防重复支付）/ 唯一约束 全是同一思想</li><li>原子 SQL 一条（不用读旧值）、版本型两条（先读快照再校验）——&quot;一条能原子解决就别上锁&quot;</li></ul><p><strong>状态单调性决定预检可靠性</strong>（自己悟的，值钱）：锁外预检只对&quot;<strong>单调状态</strong>&quot;（只增不减：已领/幂等标记）可靠、不会误杀；&quot;<strong>可回退状态</strong>&quot;（库存可退/剩余量回升）判断必须锁内拿最新值——秒杀 = &quot;是否已抢&quot;（可预检）+ &quot;还有没有货&quot;（锁内判）。</p><p><strong>锁粒度 = 并发粒度</strong>：锁 key 决定互斥范围 = 能并行多少（<code>seckill:product</code> 串行 1 vs <code>seckill:product:1</code> 并发 = 资源数）。key 粒度是<strong>性能决策不是正确性决策</strong>。</p><p><strong>悲观锁必须事务</strong>：FOR UPDATE 锁随事务提交/回滚释放，autocommit 下锁立即释放（锁个寂寞）；但&quot;事务&quot;不限于 @Transactional（TransactionTemplate/JDBC 手动都行）。</p><p><strong>Redis 分布式锁</strong>：</p><ul><li>key = 锁什么资源，<strong>value = 持有者 UUID（所有权凭证）</strong>——释放靠 Lua&quot;比对 value 才删&quot;（原子两步），防超时后误删别人的锁</li><li>Redisson 全封装：value 存 <code>UUID:threadId</code> + hash 可重入计数 + <strong>看门狗</strong>（默认 30s，业务没跑完每 10s 自动续，防提前过期）</li><li>两层过期：正常靠看门狗续、<strong>宕机靠 TTL 兜底</strong>（看门狗在持锁 JVM 里，JVM 死它也死）</li><li>锁粒度选择看&quot;临界资源独立性&quot;：资源按 id 独立就带 id；任务全局唯一（对账/刷新）用任务名</li><li>锁&quot;数据&quot; vs 锁&quot;代码&quot;：DB 锁数据行（库管），Redis 锁代码临界区（自己管令牌）</li></ul><h3 id="十五、数据批量写与主从一致-网络往返视角统一" tabindex="-1">十五、数据批量写与主从一致（网络往返视角统一） <a class="header-anchor" href="#十五、数据批量写与主从一致-网络往返视角统一" aria-label="Permalink to &quot;十五、数据批量写与主从一致（网络往返视角统一）&quot;">​</a></h3><p><strong>批量写三种方案的性能本质 = 网络往返次数</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>循环逐条（N 次往返）&lt; BATCH 攒批（addBatch 本地攒 + flushStatements 一批发，N/1000 次）&lt; 合并 SQL（1 次）</span></span>
<span class="line"><span>BATCH 三坑：手动 flush、返回值不可靠、和 @Transactional 混用冲突（session 不同）</span></span>
<span class="line"><span>JDBC url 加 rewriteBatchedStatements=true 让 MySQL 重写合并批里的 SQL</span></span></code></pre></div><p><strong>主从复制有两层一致</strong>（自己悟的）：</p><ul><li>第一层：主库并发写 → <strong>主库行锁</strong>（和单库一模一样，写全落主库）——需要锁</li><li>第二层：主库→从库 → binlog <strong>顺序重放</strong>，没有并发写 → 不用锁，一致风险是<strong>复制延迟</strong>（不是冲突）</li><li>所以&quot;主从靠复制策略不是锁&quot;指第二层（半同步/关键读走主/延迟监控）；第一层照样靠 DB 锁</li><li>四种数据形态（单库/主从/分片/分布式库）的锁与一致见 <a href="/knowledge/service/data-distribution-locks.html">数据分布场景与锁设计</a></li></ul><h3 id="十六、sql-优化动手闭环-idea-红波浪线" tabindex="-1">十六、SQL 优化动手闭环 + IDEA 红波浪线 <a class="header-anchor" href="#十六、sql-优化动手闭环-idea-红波浪线" aria-label="Permalink to &quot;十六、SQL 优化动手闭环 + IDEA 红波浪线&quot;">​</a></h3><p><strong>LEFT JOIN + 右表字段 IS NOT NULL = INNER JOIN</strong>（语义等价，过滤只针对最右表时成立）——改了 mall <code>getResourceList</code> 四表联查并测试通过（第一次&quot;看懂 → 动手改 → 验证&quot;完整闭环）。</p><p><strong>IDEA XML 红波浪线</strong> = SQL inspection 没配数据库数据源 → 猜列不存在而标红，<strong>不是 SQL 写错</strong>（运行时 MySQL 解析正常）——配 Database 数据源解决。</p><h3 id="十七、redis-认知补全" tabindex="-1">十七、Redis 认知补全 <a class="header-anchor" href="#十七、redis-认知补全" aria-label="Permalink to &quot;十七、Redis 认知补全&quot;">​</a></h3><ul><li><strong>缓存三大问题 mall 没做防御</strong>的原因：key 少（几个 admin/资源）、访问温和，够不着穿透/击穿/雪崩的触发条件——<strong>方案必要性由场景决定</strong>（高并发才值得上布隆/互斥/随机）</li><li><code>keys *</code> 生产禁用：单线程被全量遍历占死（阻塞）+ 运维 <code>rename-command KEYS &quot;&quot;</code> 禁命令；<code>scan</code> 游标分批不阻塞；<code>--scan --pattern &#39;ums:*&#39; | xargs del</code> 是清缓存正规姿势</li><li><code>@CacheException</code>（RedisCacheAspect）：Redis 挂了自己抛异常，切面决定&quot;命运&quot;——<strong>普通缓存吞异常降级（回源 DB），关键操作（验证码）必须抛</strong>（假成功比失败更糟）；降级前提 = 有第二数据源，主源在 Redis 的数据只能显式失败</li></ul><h2 id="_2026-09-04-第四篇" tabindex="-1">2026-09-04（第四篇） <a class="header-anchor" href="#_2026-09-04-第四篇" aria-label="Permalink to &quot;2026-09-04（第四篇）&quot;">​</a></h2><h3 id="十八、通知功能实战-从建表到接口的完整闭环-06-篇延伸" tabindex="-1">十八、通知功能实战：从建表到接口的完整闭环（06 篇延伸） <a class="header-anchor" href="#十八、通知功能实战-从建表到接口的完整闭环-06-篇延伸" aria-label="Permalink to &quot;十八、通知功能实战：从建表到接口的完整闭环（06 篇延伸）&quot;">​</a></h3><p>自己动手写了 cms_notice 的完整 CRUD，踩了一圈真实开发必经的坑：</p><p><strong>Swagger 看不到控制器（排查三板斧）</strong>：</p><ul><li><code>@Controller</code> ≠ <code>@RestController</code>——纯 @Controller 返回值走视图渲染，SpringDoc 不收录、返回也不是 JSON</li><li><strong>只有类级 @RequestMapping 不够，每个方法必须有映射注解</strong>（@GetMapping 等）——没有映射 = URL 不存在 = 404 且文档无 operation</li><li>排查顺序：注解 → 方法映射 → springdoc 扫描限制 → 是否重启</li></ul><p><strong>入参 DTO 与实体分离（Param 的第二个存在理由）</strong>：</p><ul><li><code>@RequestBody CmsNotice</code>（实体直接当入参）→ Swagger schema 暴露 id/createTime，调用方可伪造</li><li>正确：入参用 Param（只含调用方可给的字段），Controller/Service 转换成实体</li><li><strong>入参模型 ≠ 存储模型</strong>：id 由 DB 自增回填、createTime 由代码/DB 填，天然不该出现在&quot;新增请求&quot;里</li></ul><p><strong>DTO 字段默认值</strong>：声明时直接赋初值（<code>private String status = &quot;1&quot;;</code>）；<strong>@Builder 必须配 @Builder.Default</strong>（否则 builder 把字段初值冲成 null）。</p><p><strong>Selective 与 DB 默认值的配合（关键机制）</strong>：</p><ul><li><code>insert</code>（全字段）会把 null 显式写入 → <strong>绕过 DB 默认值</strong>；<code>insertSelective</code>（null 列不进 SQL）→ DB 默认生效</li><li><code>updateByPrimaryKeyWithBLOBs</code>（全字段 UPDATE）会把没赋值的字段<strong>刷成 NULL</strong>——update 一律用 <code>updateByPrimaryKeySelective</code>（本次实战：updateStatus 复用 update + Selective，只更新 status 列）</li><li>时间字段交给 DB：<code>create_time datetime DEFAULT CURRENT_TIMESTAMP</code>、<code>update_time ... ON UPDATE CURRENT_TIMESTAMP</code>——<strong>时间字段不是 DB 自带的</strong>（mall 76 表只有 28 个 create_time、0 个 update_time），是建表显式声明 + 列属性自动维护</li></ul><h3 id="十九、mbg-自动生成-从手养到自动养" tabindex="-1">十九、MBG 自动生成（从手养到自动养） <a class="header-anchor" href="#十九、mbg-自动生成-从手养到自动养" aria-label="Permalink to &quot;十九、MBG 自动生成（从手养到自动养）&quot;">​</a></h3><p><strong>流程</strong>：generator.properties 配库 → generatorConfig.xml 配表 → 跑 <code>Generator.main()</code> → model/Example/Mapper/xml 全套生成到 mall-mbg。</p><p><strong>三个坑</strong>：</p><ul><li><strong>Mac 路径</strong>：原配置 targetProject 是反斜杠（<code>mall-mbg\\src\\main\\java</code>，作者 Windows 开发）——Mac 上会生成到字面目录，必须改正斜杠</li><li><strong>FQCN 冲突</strong>：手写的类（mall-admin）与生成类（mall-mbg）同全限定名 → 类路径冲突，手写版必须删（Service/Controller 的 import FQCN 不变，零改动）</li><li><strong>重跑 = 覆盖</strong>：以 DB 当前结构重置生成文件——<strong>生成物永远不手改</strong>；丢自定义用 IDEA Local History 找回</li></ul><p><strong>铁律</strong>：<code>model/mapper/xml = 生成物（改表重跑）</code>、<code>Service/Controller = 手写区</code>；自定义查询学 mall 的 <code>UmsAdminRoleRelationDao</code> 模式（自定义 dao 与生成 mapper 并存）。</p><h3 id="二十、spring-bean-概念链-一次串清" tabindex="-1">二十、Spring Bean 概念链（一次串清） <a class="header-anchor" href="#二十、spring-bean-概念链-一次串清" aria-label="Permalink to &quot;二十、Spring Bean 概念链（一次串清）&quot;">​</a></h3><ul><li><strong>Bean = 交给 Spring 容器创建和管理的对象</strong>（IoC：对象不自己 new 依赖，容器统一创建按需注入，默认单例）</li><li><strong>@Component 家族</strong>（类注解，扫描注册）：@Service/@Repository/@Controller 是 @Component 的语义化派生；@Repository 多异常翻译、@Controller 多 Web 能力</li><li><strong>@Bean</strong>（方法注解，手动注册）：用于&quot;没法加类注解&quot;的对象——接口适配的方法引用（<code>adminService::loadUserByUsername</code>）、第三方类</li><li><strong>@Autowired 按类型从统一容器找</strong>：跨模块/跨 @Configuration 都能注入；前提该类型 Bean 唯一（多个报 NoUniqueBeanDefinitionException）；注入先于 @PostConstruct</li><li><strong>@PostConstruct = 依赖注入完成后的初始化钩子</strong>（构造器里依赖还是 null；对应前端 useEffect([])/onMounted）——DynamicSecurityMetadataSource 启动预加载权限 Map 就是它</li><li><strong>安全装配层模式</strong>：MallSecurityConfig = &quot;通用库定义接口（mall-security 不绑业务）+ 应用侧适配注册（把业务 Service 方法适配成框架接口）&quot;，两条链路（认证/鉴权）的扩展点统一在这注册</li></ul><p><strong>record（Java 16 语言特性，非 Spring）</strong>：一行生成不可变数据类全件（final 字段/全参构造/访问器 name()/equals/hashCode/toString）；适合<strong>只读 DTO</strong>；compact constructor 可加校验+防御拷贝；<strong>有状态组件别转</strong>（IDEA 会误报 Convert to record）；判断归属口诀&quot;不带 spring 依赖能用 = Java 语言&quot;。</p><h3 id="二十一、权限与缓存实战踩坑" tabindex="-1">二十一、权限与缓存实战踩坑 <a class="header-anchor" href="#二十一、权限与缓存实战踩坑" aria-label="Permalink to &quot;二十一、权限与缓存实战踩坑&quot;">​</a></h3><ul><li><strong>allocResource 是&quot;全量分配&quot;接口</strong>（先删后插）——传 [33] 的语义是&quot;角色 5 只有 33&quot;，不是追加 → 实际把角色 5 资源清空了。追加 = 传&quot;现有全部 + 新增&quot;</li><li><strong>恢复数据用批量 INSERT 不带 id</strong>（自增分配无冲突风险）：<code>INSERT INTO t (role_id, resource_id) VALUES (5,1),(5,2)...</code>；逐条复制执行容易丢分号（1064 两条连成一条）</li><li><strong>改权限库后必须清 <code>ums:resourceList:*</code> 缓存</strong>（登录用户权限列表缓存在 Redis，不清则旧权限一直生效）</li><li><strong>白名单 403 的通配符坑</strong>：<code>/notice/*</code> 只匹配一段（/notice/list ✓），<strong>不匹配 /notice 本身</strong>（insert 的 URL）——要用 <code>/notice/**</code>（** = 零或多段）。口诀：* 一段，** 剩余全部</li><li>动态权限规则：<strong>URL 未在 ums_resource 登记并授权 → 一律 403</strong>（不是&quot;没登记就放行&quot;）</li></ul><h3 id="二十二、杂项认知速记" tabindex="-1">二十二、杂项认知速记 <a class="header-anchor" href="#二十二、杂项认知速记" aria-label="Permalink to &quot;二十二、杂项认知速记&quot;">​</a></h3><ul><li><strong>keys * 生产不能用</strong>：单线程被全量遍历占死 + 运维 <code>rename-command KEYS &quot;&quot;</code> 禁用；<code>scan</code> 游标分批不阻塞；清缓存正规姿势 <code>redis-cli --scan --pattern &#39;ums:*&#39; | xargs del</code></li><li><strong>TEXT = 65535 字节 ≈ 64KB</strong>（utf8mb4 约 1.6 万汉字）；富文本（图片外链）够用，含 base64 图要 MEDIUMTEXT；1MB = 1024KB 是存储进制（硬盘厂商按 1000）</li><li><strong>int(11) 的括号被 MySQL 8 弃用</strong>：显示宽度不影响范围（int 固定 4 字节），新表写 <code>int</code> 即可</li><li><strong>model 的 setId 能改</strong>：Java 实体只是表结构的内存映射，自增/主键约束在 DB 层；纪律是 insert 别手动传 id、已持久化记录别改 id</li><li><strong>DDL 的 Updated Rows 0 = 正常</strong>（改结构不改数据）；验证用 SHOW CREATE TABLE</li><li><strong>LEFT JOIN + 右表字段 IS NOT NULL = INNER JOIN</strong>（已动手改 mall 的 getResourceList 四表联查并测试）</li></ul><hr><h2 id="待补充话题" tabindex="-1">待补充话题 <a class="header-anchor" href="#待补充话题" aria-label="Permalink to &quot;待补充话题&quot;">​</a></h2><ul><li>登出方案落地（时间戳黑名单 / token 黑名单二选一）</li><li>阶段 6 动手开发的完整流程收获</li></ul>`,121)])])}const u=e(i,[["render",n]]);export{h as __pageData,u as default};
