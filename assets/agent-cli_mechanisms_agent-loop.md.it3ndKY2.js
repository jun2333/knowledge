import{_ as t,o as e,c as a,a4 as s}from"./chunks/framework.BBs01uty.js";const g=JSON.parse('{"title":"机制二：Agent 循环（src/agent/loop.ts）","description":"","frontmatter":{},"headers":[],"relativePath":"agent-cli/mechanisms/agent-loop.md","filePath":"agent-cli/mechanisms/agent-loop.md"}'),n={name:"agent-cli/mechanisms/agent-loop.md"};function d(l,o,c,i,r,p){return e(),a("div",null,[...o[0]||(o[0]=[s(`<h1 id="机制二-agent-循环-src-agent-loop-ts" tabindex="-1">机制二：Agent 循环（<code>src/agent/loop.ts</code>） <a class="header-anchor" href="#机制二-agent-循环-src-agent-loop-ts" aria-label="Permalink to &quot;机制二：Agent 循环（\`src/agent/loop.ts\`）&quot;">​</a></h1><blockquote><p>195 行的小模块，却是整个项目的&quot;发动机&quot;。它把 LLM 的流式响应变成<strong>事件流</strong>， 上层（<code>index.ts</code>）消费事件去渲染 / 执行工具 / 回传结果。</p></blockquote><h2 id="_1-接口形状" tabindex="-1">1. 接口形状 <a class="header-anchor" href="#_1-接口形状" aria-label="Permalink to &quot;1. 接口形状&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">runAgentLoop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  messages, definitions, implementations, client,   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// OpenAI 兼容 client（指向 Ollama /v1）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  model, maxIterations </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, signal, think,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}): AsyncGenerator</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LoopEvent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p><code>index.ts</code> 用 <code>for await (const event of runAgentLoop(...))</code> 消费。<strong>loop 只发事件，不做 UI</strong> —— 渲染、工具行的 ✓/✗、审批弹窗全在上层。这让它可以被纯单测（mock 一个假流）。</p><h2 id="_2-事件契约-b1-修正版" tabindex="-1">2. 事件契约（B1 修正版） <a class="header-anchor" href="#_2-事件契约-b1-修正版" aria-label="Permalink to &quot;2. 事件契约（B1 修正版）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>事件</th><th>载荷</th><th>语义 / 上层动作</th></tr></thead><tbody><tr><td><code>status</code></td><td><code>phase: &#39;llm_start&#39;</code></td><td>状态栏迁移</td></tr><tr><td><code>reasoning</code></td><td><code>content</code>（增量）</td><td><code>updateThinking</code> → 思考面板</td></tr><tr><td><code>token</code></td><td><code>content</code>（增量）</td><td><code>appendToLast</code> → 正文追加</td></tr><tr><td><code>tool_start</code></td><td><code>callId</code> <code>name</code> <code>args</code></td><td><code>transitionStatus(tool_start)</code> + <code>beginToolLine</code>（<strong>开始即渲染</strong>，AC-2）</td></tr><tr><td><code>tool_end</code></td><td><code>ok</code> <code>durationMs</code> <code>outputLines</code> <code>outputBytes</code></td><td><code>endToolLine</code> 同行原地变完成态（AC-3，D16）</td></tr><tr><td><code>error</code></td><td><code>message</code></td><td><code>showError</code> + 状态栏 error</td></tr></tbody></table><p>⚠️ 三个曾经踩过的坑（都写进了 <code>loop.ts</code> 头注释）：</p><ol><li><code>tool_start</code> <strong>只 emit 一次</strong>且在 <code>await fn(args)</code> <strong>之前</strong>，必带 <code>callId/name/args</code> —— 旧实现 emit 两次、第二次不带 name，上层把工具名清成空（AC-2 反例）。</li><li><code>tool_end</code> 必须存在并携带耗时/行数/字节 —— 否则 &quot;Running tool&quot; 会残留到下一次 <code>llm_start</code>。</li><li><code>finish_reason=&#39;length&#39;</code>（长思考 + 图片极易触发）时模型<strong>既无 content 也无 tool_calls</strong> —— 旧实现直接 return，用户只看到&quot;思考着就停了&quot;。现在会发 <code>error</code> 提示（&quot;可重试，或先用 /model 降低思考等级&quot;）。</li></ol><h2 id="_3-流式-tool-calls-的增量累积-最容易写错的地方" tabindex="-1">3. 流式 <code>tool_calls</code> 的增量累积（最容易写错的地方） <a class="header-anchor" href="#_3-流式-tool-calls-的增量累积-最容易写错的地方" aria-label="Permalink to &quot;3. 流式 \`tool_calls\` 的增量累积（最容易写错的地方）&quot;">​</a></h2><p>OpenAI 兼容流里，<code>delta.tool_calls</code> 是<strong>分片</strong>返回的：同一次调用的函数名和参数字符串被拆成多个 delta， 只带 <code>index</code>（可能有首片带 <code>id</code>）。必须<strong>按 index 累积拼接</strong>，流结束后才能 <code>JSON.parse</code> 参数：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>delta.tool_calls: [{index:0, id, function:{name:&#39;bash&#39;}}]</span></span>
<span class="line"><span>                  [{index:0, function:{arguments:&#39;{&quot;co&#39;}}]</span></span>
<span class="line"><span>                  [{index:0, function:{arguments:&#39;mmand&quot;:&quot;ls&quot;}&#39;}}]</span></span>
<span class="line"><span>→ 累积后：{ id, name:&#39;bash&#39;, arguments:&#39;{&quot;command&quot;:&quot;ls&quot;}&#39; }</span></span></code></pre></div><p>累积完成后把 assistant 消息（含完整 <code>tool_calls</code>）回传 messages，再逐个执行工具。</p><h2 id="_4-迭代与终止" tabindex="-1">4. 迭代与终止 <a class="header-anchor" href="#_4-迭代与终止" aria-label="Permalink to &quot;4. 迭代与终止&quot;">​</a></h2><ul><li><code>for (turn &lt; maxIterations)</code>，默认 <code>config.maxIterations = 5</code>。</li><li>每轮：流式收集 → 有 <code>tool_calls</code>？→ 执行 + 回传 → 下一轮；无 → 结束生成器。</li><li>支持 <code>signal</code>（用户 Esc 打断 → <code>AbortController</code>）。</li></ul><h2 id="_5-工具执行在哪里" tabindex="-1">5. 工具执行在哪里 <a class="header-anchor" href="#_5-工具执行在哪里" aria-label="Permalink to &quot;5. 工具执行在哪里&quot;">​</a></h2><p><strong>loop 不自己执行工具</strong>：<code>tool_start</code> 事件发出后，由上层（<code>index.ts</code>）调用注入的 <code>implementations[name](args)</code>（即 <code>tools/executor.ts</code> 包装过的实现，含权限 / Hook / 截断，见 <a href="./tools.html"><code>tools.md</code></a>），完成后 loop 继续发 <code>tool_end</code> 并把结果作为 <code>tool</code> 消息回传。</p><blockquote><p>这个&quot;loop 发事件、上层执行&quot;的拆分是为了让工具执行能弹审批 UI、跑 Hook—— 这些副作用不属于&quot;解析流式响应&quot;的职责。</p></blockquote><h2 id="_6-测试" tabindex="-1">6. 测试 <a class="header-anchor" href="#_6-测试" aria-label="Permalink to &quot;6. 测试&quot;">​</a></h2><p><code>agent/loop.test.ts</code>：注入假 OpenAI client（可控的 chunk 序列）， 断言事件序列、tool_calls 分片累积、&quot;静默失败&quot;分支、maxIterations 上限。</p>`,20)])])}const u=t(n,[["render",d]]);export{g as __pageData,u as default};
