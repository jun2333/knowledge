import{_ as e,o as d,c as o,a4 as c}from"./chunks/framework.BBs01uty.js";const u=JSON.parse('{"title":"模块清单","description":"","frontmatter":{},"headers":[],"relativePath":"agent-cli/modules.md","filePath":"agent-cli/modules.md"}'),s={name:"agent-cli/modules.md"};function r(a,t,n,i,l,h){return d(),o("div",null,[...t[0]||(t[0]=[c('<h1 id="模块清单" tabindex="-1">模块清单 <a class="header-anchor" href="#模块清单" aria-label="Permalink to &quot;模块清单&quot;">​</a></h1><blockquote><p><code>src/</code> 全部 35 个生产模块（共 12,131 行）逐一清点。 每个模块文件<strong>开头都有一段&quot;为什么这么设计 / 职责边界&quot;的中文注释</strong> —— 那是比本文更权威的事实来源； 本文是索引，改代码时两头都要同步。</p></blockquote><p>图例：<strong>职责</strong> = 它负责什么；<strong>关键导出</strong> = 常用 API；<strong>依赖</strong> = 主要协作方；<strong>测试</strong> = 对应测试文件。</p><hr><h2 id="入口-编排" tabindex="-1">入口 / 编排 <a class="header-anchor" href="#入口-编排" aria-label="Permalink to &quot;入口 / 编排&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>index.ts</code></td><td style="text-align:right;">1615</td><td>程序入口：参数解析、system prompt 组装、<strong>命令表</strong>、斜杠命令、<code>!</code> bash 模式、Agent 循环的<strong>事件消费者</strong>、退出收尾编排。也是&quot;唯一接线处&quot;（任务面板/常驻行数据源、权限、Hook 都在这里装配）</td><td><code>startInteractive</code> <code>runTurn</code> <code>createShutdown</code> <code>createApprovalRequest</code> <code>HELP_TEXT</code> <code>projectPermissionsWriter</code> <code>historyTextOf</code> <code>bangContextMessage</code></td><td>几乎所有模块</td><td><code>index.e2e.test.ts</code>（2642）<code>index.noninteractive*.e2e.test.ts</code> <code>index.exit-signal.e2e.test.ts</code></td></tr><tr><td><code>config.ts</code></td><td style="text-align:right;">35</td><td>全局配置常量与项目根：<code>projectRoot = process.cwd()</code>（工具作用于&quot;启动目录&quot;，同 Claude CLI）；Ollama 地址 / 默认模型 / 各类阈值</td><td><code>projectRoot</code> <code>config</code></td><td>—</td><td>—</td></tr></tbody></table><h2 id="agent-层" tabindex="-1">Agent 层 <a class="header-anchor" href="#agent-层" aria-label="Permalink to &quot;Agent 层&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>agent/loop.ts</code></td><td style="text-align:right;">195</td><td>Agent 循环核心：调 LLM 流式接口，<strong>按 index 增量累积被分片的 <code>tool_calls</code></strong>，emit 统一事件契约（<code>llm_start</code> / <code>reasoning</code> / <code>token</code> / <code>tool_start</code> / <code>tool_end</code> / <code>error</code>），执行工具并把结果回传下一轮</td><td><code>runAgentLoop</code>（异步生成器）</td><td><code>tools/</code> <code>interaction</code> <code>permissions</code></td><td><code>agent/loop.test.ts</code></td></tr><tr><td><code>models.ts</code></td><td style="text-align:right;">196</td><td>Ollama 模型清单与能力探测：<code>ollama list</code> 无能力字段，故对每个模型再调 <code>/api/show</code> 取 <code>capabilities</code>；<strong>过滤 embedding 类的唯一判据 = capabilities 不含 <code>completion</code></strong>；探测失败返回 <code>null</code> 表示&quot;未知&quot;，此时<strong>保守保留</strong>模型。网络/子进程调用收在可注入的 <code>ModelsDeps</code> 里</td><td><code>setModelsDeps</code> <code>resetModelsDeps</code></td><td><code>config</code></td><td><code>models.test.ts</code></td></tr></tbody></table><h2 id="工具层-src-tools" tabindex="-1">工具层（<code>src/tools/</code>） <a class="header-anchor" href="#工具层-src-tools" aria-label="Permalink to &quot;工具层（`src/tools/`）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>tools/index.ts</code></td><td style="text-align:right;">70</td><td><strong>薄聚合门面</strong>（重构后）：对外只暴露工具层入口，不再内联 schema 与实现</td><td>聚合导出</td><td><code>executor</code> <code>registry</code></td><td><code>tools/tools.test.ts</code></td></tr><tr><td><code>tools/registry.ts</code></td><td style="text-align:right;">1065</td><td><strong>15 个工具的唯一定义处</strong>：<code>meta</code>(name/description/parameters) + <code>impl</code>。含 <code>bash</code>（含 <code>run_in_background</code>）、<code>bash_output</code>、<code>kill_task</code>、<code>ask_user</code>、memory 三件套、文件四件套、<code>web_search</code>、<code>view_image</code></td><td><code>TOOL_SPECS</code> <code>buildDefinitions</code> <code>classifyToolResult</code></td><td><code>permissions</code> <code>tasks</code> <code>interaction</code></td><td><code>tools/tools.test.ts</code></td></tr><tr><td><code>tools/executor.ts</code></td><td style="text-align:right;">291</td><td>工具执行编排：按名称查表 → 权限判定 → Hook <code>PreToolUse</code> → 执行 → <code>PostToolUse</code> → 输出经 <code>truncate</code> 收敛</td><td><code>createToolExecutor</code></td><td><code>registry</code> <code>permissions</code> <code>hooks</code> <code>truncate</code></td><td><code>tools/executor.test.ts</code></td></tr><tr><td><code>tools/truncate.ts</code></td><td style="text-align:right;">222</td><td><strong>输出规模口径的唯一实现</strong>（lesson 012）：行数 <code>countLines()</code>（按 <code>\\r\\n|\\r|\\n</code> 切分，末尾行尾不额外算一行）、字节数、<strong>双阈值截断</strong>（先按行再按字节）。阈值 / 标注 / &quot;输出 N 行&quot; 三者同源</td><td><code>countLines</code> <code>measureToolOutput</code> <code>truncateToolOutput</code> <code>truncateToolResult</code></td><td>—</td><td><code>tools/truncate.test.ts</code></td></tr><tr><td><code>tools/definitions.baseline.ts</code></td><td style="text-align:right;">279</td><td><strong>AC-41 基线快照</strong>：重构前（B2 之前）<code>toolDefinitions</code> 的真实序列化产出（脚本生成、非人工抄写，附 sha256）。用于断言&quot;重构后 definitions 不变&quot;</td><td>基线常量</td><td>—</td><td><code>tools/tools.test.ts</code></td></tr></tbody></table><h2 id="权限-hook" tabindex="-1">权限 / Hook <a class="header-anchor" href="#权限-hook" aria-label="Permalink to &quot;权限 / Hook&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>permissions.ts</code></td><td style="text-align:right;">261</td><td>权限判定与模式：<strong>判定是纯函数</strong> <code>decidePermission(meta, args, mode)</code>（无 IO / 无全局状态），4 种模式 <code>default</code>/<code>acceptEdits</code>/<code>plan</code>/<code>bypass</code>；<code>PermissionController</code> 持有模式并<strong>按引用</strong>注入 executor（<code>/mode</code> 改同一对象，下次工具调用即生效）；会话级授权（<code>grantSessionEdit</code> / <code>grantSessionBash</code>）不落盘。<strong>I-3 不变式</strong>：<code>ask &amp;&amp; forced</code> 在任何授权前短路</td><td><code>decidePermission</code> <code>PermissionController</code> <code>nextPermissionMode</code> <code>PermissionMode</code></td><td>—</td><td><code>permissions.test.ts</code></td></tr><tr><td><code>project-permissions.ts</code></td><td style="text-align:right;">199</td><td><strong>项目级权限文件</strong>（<code>&lt;仓库根&gt;/.agent-cli/permissions.json</code>）：仓库根解析（git，非 git 回落 cwd）、三态加载（none/ok/corrupt，损坏仅告警不阻断）、<code>appendAllowRule</code> 原子写盘（tmp + rename，立即写）、内容 hash（信任判定用）、<code>commandFirstWord</code> 保守首词匹配（<strong>仅绝对路径取 basename</strong>，相对路径拒绝）</td><td><code>resolveRepoRoot</code> <code>permissionsFilePath</code> <code>loadProjectPermissions</code> <code>appendAllowRule</code> <code>permissionsHash</code> <code>commandFirstWord</code> <code>createPermissionStore</code></td><td><code>hooks</code></td><td><code>project-permissions.test.ts</code></td></tr><tr><td><code>hooks.ts</code></td><td style="text-align:right;">481</td><td>Hook 系统：<strong>4 个时机</strong> <code>PreToolUse</code>/<code>PostToolUse</code>/<code>UserPromptSubmit</code>/<code>Stop</code>（<strong>不做 <code>SessionStart</code></strong> —— 它会改写已发送内容，违反前缀稳定性）。协议与 Claude Code 同构：stdin 收 JSON，stdout 可回 <code>{decision:&#39;block&#39;, reason}</code>；失败语义（D33）：<code>PreToolUse</code> 非 0/超时 → <strong>阻塞</strong>，其余 → <strong>放行 + 告警</strong>。含项目级 hook 的<strong>信任</strong>判定（hash 写入 user config）</td><td>Hook runner <code>resolveProjectHooksPath</code> <code>hooksConfigHash</code> <code>trustProjectHooks</code> <code>isProjectHooksTrusted</code></td><td><code>project-permissions</code></td><td><code>hooks.test.ts</code></td></tr></tbody></table><h2 id="后台任务" tabindex="-1">后台任务 <a class="header-anchor" href="#后台任务" aria-label="Permalink to &quot;后台任务&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出 / 方法</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>tasks.ts</code></td><td style="text-align:right;">715</td><td><code>TaskManager</code>：<code>spawn(&#39;sh -c&#39;, detached)</code> 建进程组（pgid = pid，便于整组回收）；环形内存缓冲（2000 行 / 256KB）+ 落盘 <code>tasks/&lt;id&gt;.log</code>；<strong>全事件驱动，不引定时器</strong>（<code>waitForExit</code> 复用 <code>exitWaiters</code>）。<br><code>start(command, {background})</code> · <code>kill</code>(幂等, SIGTERM→SIGKILL) · <code>killAll</code> · <code>waitForExit</code> · <code>tailOutput</code> · <code>refreshJobStates</code>(作业级收尾) · <code>markBackground</code>(Ctrl+B 转后台) · <code>forget</code>/<code>forgetFinished</code>(真移除)</td><td><code>TaskManager</code> <code>TaskRecord</code> <code>tasksDir</code> <code>TASK_ID_PATTERN</code></td><td><code>images</code></td><td><code>tasks.test.ts</code></td></tr><tr><td><code>ui/task-panel.ts</code></td><td style="text-align:right;">265</td><td>任务<strong>面板的纯逻辑</strong>：条目单一形状 + <code>kind</code> 标签（<strong>渲染绝不出现 <code>kind</code> 分支</strong>，AC-18）；状态徽标/配色映射表；键盘语义（↑↓ / Enter 详情 / <code>x</code> 关闭 / <code>c</code> 清理）；<code>canOpenTaskPanel</code> 是 ↓ 打开面板的<strong>唯一判定处</strong>（纯谓词）。高度冻结 10 行（V-5）</td><td><code>createTaskPanel</code> <code>renderTaskPanel</code> <code>handleTaskPanelKey</code> <code>entryFromTask</code> <code>canOpenTaskPanel</code> <code>STATE_BADGE</code> <code>TASK_PANEL_HEIGHT</code> <code>TaskPanelSource</code></td><td><code>text</code> <code>tasks</code>(类型)</td><td><code>ui/task-panel.test.ts</code></td></tr><tr><td><code>ui/fullscreen.ts</code></td><td style="text-align:right;">136</td><td>任务<strong>详情全屏视图</strong>（AC-15）：首行头部（状态/id/命令/时长/行数）+ 输出视口；构造接收 <code>TaskDetailInput</code> <strong>或读取器函数</strong>（实时模式：每次渲染重读）；默认<strong>贴底跟随最新输出</strong>，手动上滚脱离、回底恢复</td><td><code>TaskDetailView</code> <code>FullscreenView</code> <code>TASK_DETAIL_TAIL_LINES</code></td><td><code>task-panel</code> <code>text</code></td><td><code>ui/fullscreen.test.ts</code></td></tr></tbody></table><h2 id="交互原语-fr-5" tabindex="-1">交互原语（FR-5） <a class="header-anchor" href="#交互原语-fr-5" aria-label="Permalink to &quot;交互原语（FR-5）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>interaction.ts</code></td><td style="text-align:right;">260</td><td><strong>交互原语的分派中枢</strong>（AC-27）：权限审批、<code>/jobs</code>、Hook 信任、LLM 的 <code>ask_user</code> <strong>四个消费者共用同一实现</strong>。<code>InteractionBroker</code> 把&quot;打开 UI → 回调转 Promise&quot;收敛成一处；非交互/<code>--yes</code> 下有 4 种降级分支（V-1）</td><td><code>InteractionBroker</code> <code>InteractionSpec</code></td><td><code>ui/tui</code> <code>ui/overlay</code></td><td><code>interaction.test.ts</code></td></tr><tr><td><code>ui/overlay.ts</code></td><td style="text-align:right;">160</td><td><code>InteractionSpec → OverlaySession</code> 的<strong>纯适配器</strong>：把旧三形态（confirm / select / input）<strong>折叠</strong>成 questions 规格，<code>questions</code> 原生形态直接透传；再把 questions 答案回填成旧的 <code>OverlaySubmit</code> 兼容壳</td><td><code>createSpecSession</code> <code>specToQuestions</code> <code>mapAnswer</code> <code>MAX_OVERLAY_ITEMS</code></td><td><code>overlay-questions</code> <code>overlay-session</code></td><td><code>ui/overlay.test.ts</code></td></tr><tr><td><code>ui/overlay-questions.ts</code></td><td style="text-align:right;">422</td><td><strong>通用弹层（questions）引擎 —— 浮层能力的唯一实现</strong>：1~4 问分页、「其他…」内联手输、multiSelect、类型化选项；<strong>高度在生命周期内恒定</strong>（标题创建时折行冻结）；入口处统一净化 spec 文本（安全边界）</td><td><code>createQuestionsSession</code> <code>renderQuestions</code> <code>handleQuestionsKey</code> <code>questionsHeight</code> <code>effectiveOptions</code> <code>QuestionsSpec</code></td><td><code>text</code> <code>overlay-session</code></td><td><code>ui/overlay-questions.test.ts</code></td></tr><tr><td><code>ui/overlay-session.ts</code></td><td style="text-align:right;">91</td><td>浮层会话契约与约束：<code>OverlaySession</code>（id / 冻结 height / render / handleKey / onOpen / onClose）、<code>assertSessionHeight</code>（渲染前校验行数恒等于 height）、阻塞型会话判定 <code>isBlockingSession</code>（内部 <code>BLOCKING_SESSION_IDS = {confirm, ask, trust, approval}</code>）</td><td><code>OverlaySession</code> <code>assertSessionHeight</code> <code>isBlockingSession</code> <code>OverlayKeyOutcome</code></td><td>—</td><td><code>ui/overlay-session.test.ts</code></td></tr></tbody></table><h2 id="ui-层-src-ui" tabindex="-1">UI 层（<code>src/ui/</code>） <a class="header-anchor" href="#ui-层-src-ui" aria-label="Permalink to &quot;UI 层（`src/ui/`）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>ui/tui.ts</code></td><td style="text-align:right;">2099</td><td><strong>自研 DECSTBM 终端渲染层</strong>：布局唯一公式（<code>contentRowsFor</code> / <code>reservedRowsFor</code>）、固定区/滚动区/输入框/常驻行/浮层块绘制、按键路由、粘贴、resize 重排、全屏视图与浮层的宿主、<strong>唯一 <code>write</code> 出口</strong></td><td><code>TUI</code> <code>contentRowsFor</code> <code>reservedRowsFor</code> <code>TUIHandlers</code></td><td>大部分 <code>ui/*</code></td><td><code>ui/tui.e2e.test.ts</code>（2642）</td></tr><tr><td><code>ui/input-buffer.ts</code></td><td style="text-align:right;">523</td><td><strong>输入框缓冲的纯逻辑</strong>：原子数组模型（图片/粘贴占位符是独立元素，<strong>一次 backspace 整体删除</strong>，且图片在消息里的位置与文本位置天然一致）；光标移动、折行、裁剪</td><td><code>InputBuffer</code></td><td><code>text</code></td><td><code>ui/input-buffer.test.ts</code></td></tr><tr><td><code>ui/status-machine.ts</code></td><td style="text-align:right;">274</td><td>状态栏的<strong>显式状态机 + 唯一定时器</strong>：迁移表可单测穷举（AC-1）；<code>Ticker</code> 惰性启停（idle 必停，AC-4）；动画帧由 elapsed 派生（无内部计数器）</td><td><code>StatusMachine</code> <code>Ticker</code> <code>spinnerFrame</code> <code>FRAME_INTERVAL_MS</code>(100)</td><td>—</td><td><code>ui/status-machine.test.ts</code></td></tr><tr><td><code>ui/completion-menu.ts</code></td><td style="text-align:right;">228</td><td>斜杠补全菜单的纯逻辑：候选窗口、过滤后选中重置、<code>Tab</code> 填入 / <code>Enter</code> 执行 / <code>Esc</code> 关闭（不清输入，AC-22）；<code>prefix</code> 区分 <code>/</code> 与 <code>!</code> 历史；高度恒定</td><td><code>createCompletionMenu</code> <code>renderCompletionMenu</code> <code>handleMenuKey</code> <code>isCompletionTrigger</code> <code>updateQuery</code></td><td><code>text</code></td><td><code>ui/completion-menu.test.ts</code></td></tr><tr><td><code>ui/text.ts</code></td><td style="text-align:right;">155</td><td>终端文本的纯函数工具：显示宽度（中文 2 列）、折行、截断、<strong><code>sanitizeForTerminal</code>（安全净化唯一实现）</strong>、tail、行内 Markdown</td><td><code>displayWidth</code> <code>wrapText</code> <code>truncateTo</code> <code>tailByWidth</code> <code>inlineMarkdown</code> <code>sanitizeForTerminal</code></td><td>—</td><td><code>ui/tui-format.test.ts</code></td></tr><tr><td><code>ui/info-line.ts</code></td><td style="text-align:right;">64</td><td>输入框下方<strong>常驻 1 行</strong>的渲染：权限模式徽标 + 任务摘要（FR-2）</td><td><code>renderInfoLine</code> <code>MODE_BADGE</code></td><td><code>text</code></td><td><code>ui/info-line.test.ts</code></td></tr><tr><td><code>ui/exit-guard.ts</code></td><td style="text-align:right;">67</td><td><code>Ctrl+C</code> 的两段式退出状态机：首次清空输入（并提示后台任务数）、再次退出（AC-38~41）；后台任务<strong>不计入 busy</strong>（V-4）</td><td><code>ExitGuard</code></td><td>—</td><td><code>ui/exit-guard.test.ts</code></td></tr><tr><td><code>ui/selector.ts</code></td><td style="text-align:right;">155</td><td>全屏模态选择器（曾供 <code>/model</code> 选模型/等级）⚠️ <strong>当前是死代码</strong>：<code>/model</code> 已改为弹层（V-1），生产零调用，<code>mode:&#39;selector&#39;</code> 仅测试可达</td><td><code>renderSelector</code> 等</td><td>—</td><td>—</td></tr></tbody></table><h2 id="命令-技能" tabindex="-1">命令 / 技能 <a class="header-anchor" href="#命令-技能" aria-label="Permalink to &quot;命令 / 技能&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>commands.ts</code></td><td style="text-align:right;">163</td><td>斜杠命令与补全候选的<strong>纯逻辑</strong>：候选从哪来、怎么过滤（<code>filterCandidates</code>）、怎么注册源（<code>CompletionSource</code>）；<code>prefix</code> 区分 <code>/</code> 与 <code>!</code> 历史源</td><td><code>CommandSpec</code> <code>CompletionCandidate</code> <code>CompletionSource</code> <code>filterCandidates</code> <code>builtinCandidates</code> <code>createBangHistorySource</code></td><td><code>skills</code></td><td><code>commands.test.ts</code></td></tr><tr><td><code>skills.ts</code></td><td style="text-align:right;">237</td><td>技能的<strong>最小发现层</strong>（V-6）：扫描 <code>~/.agent-cli/skills/</code> 与 <code>&lt;项目&gt;/.agent-cli/skills/</code>，解析 frontmatter <code>name</code> 注册为补全候选。<strong>仅发现</strong>，不含索引注入 / <code>read_skill</code> / <code>write_skill</code>（留给 P4）</td><td>技能发现与候选源</td><td><code>commands</code></td><td><code>skills.test.ts</code></td></tr></tbody></table><h2 id="基础设施" tabindex="-1">基础设施 <a class="header-anchor" href="#基础设施" aria-label="Permalink to &quot;基础设施&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th><th>关键导出</th><th>依赖</th><th>测试</th></tr></thead><tbody><tr><td><code>session.ts</code></td><td style="text-align:right;">425</td><td>会话持久化（<strong>按工作目录隔离</strong> + 会话锁）+ 根提示词 / memory 读写。保存时机：每条用户消息与每次回答后<strong>实时写盘</strong>；不存 system prompt；图片落 <code>image_ref</code> 不内联 base64；锁靠 pid 存活探测判过期</td><td><code>loadSession</code> <code>saveSession</code> <code>acquireSessionLock</code> <code>releaseSessionLock</code> <code>listSessions</code> <code>newSessionId</code> <code>sessionWorkspaceDir</code></td><td><code>images</code></td><td><code>session.test.ts</code></td></tr><tr><td><code>user-config.ts</code></td><td style="text-align:right;">122</td><td>用户配置持久化（<code>~/.agent-cli/config.json</code>）：当前模型、思考等级、<code>trustedProjects</code> / <code>trustedHooks</code>。<strong>文件不存在/损坏返回 <code>{}</code> 不抛</strong>；写入失败静默降级</td><td><code>loadUserConfig</code> <code>saveUserConfig</code> <code>UserConfig</code></td><td>—</td><td><code>user-config.test.ts</code></td></tr><tr><td><code>images.ts</code></td><td style="text-align:right;">186</td><td>图片落盘 / data URL 编码 / <strong>路径白名单（安全边界）</strong> / 按数量清理（≤100 张，单图 ≤10MB）</td><td><code>saveImage</code> <code>IMAGE_DIR_NAME</code> 白名单</td><td><code>config</code></td><td><code>images.test.ts</code></td></tr><tr><td><code>clipboard.ts</code></td><td style="text-align:right;">212</td><td>macOS 剪贴板取图 / 取文本（<code>Ctrl+V</code> 直投图片）：优先 <code>pngpaste</code>，降级 <code>osascript</code>；失败区分 <code>unavailable</code> / 空 / 非图片三种语义；依赖可注入（<code>setClipboardDeps</code>）以便单测</td><td><code>readClipboardImage</code> <code>readClipboardText</code> <code>classifyFailure</code> <code>setClipboardDeps</code></td><td><code>images</code></td><td><code>clipboard.test.ts</code></td></tr><tr><td><code>editor.ts</code></td><td style="text-align:right;">103</td><td><code>Ctrl+G</code> 外部编辑器撰写提示词：当前输入 → 临时 md → <code>$EDITOR</code>（其次 <code>$VISUAL</code>，缺省 <code>code --wait</code>）等待退出 → 读回 → 删临时文件；文件名带随机后缀避免同毫秒碰撞</td><td><code>editInExternalEditor</code></td><td><code>config</code></td><td><code>editor.test.ts</code></td></tr></tbody></table><h2 id="测试基础设施" tabindex="-1">测试基础设施 <a class="header-anchor" href="#测试基础设施" aria-label="Permalink to &quot;测试基础设施&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th style="text-align:right;">行</th><th>职责</th></tr></thead><tbody><tr><td><code>testing/term-sim.ts</code></td><td style="text-align:right;">160</td><td><strong>ANSI 终端模拟器</strong>：解析 CSI（光标定位 / erase / DECSTBM 滚动区 / 清屏清历史），维护屏幕字符矩阵 + scrollback，支持 resize / 宽字符（中文 2 列）/ wrap-pending / 滚动区滚动。让 TUI 的&quot;画面&quot;可被断言 —— 见 <a href="./testing.html"><code>testing.md</code></a>。</td></tr></tbody></table><hr><h2 id="附-规模与分布" tabindex="-1">附：规模与分布 <a class="header-anchor" href="#附-规模与分布" aria-label="Permalink to &quot;附：规模与分布&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>生产代码 12,131 行 / 35 模块</span></span>\n<span class="line"><span>测试代码 14,687 行（&gt; 生产代码）</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>最大模块：ui/tui.ts 2099 · index.ts 1615 · tools/registry.ts 1065 · tasks.ts 715 · ui/input-buffer.ts 523</span></span></code></pre></div>',27)])])}const p=e(s,[["render",r]]);export{u as __pageData,p as default};
