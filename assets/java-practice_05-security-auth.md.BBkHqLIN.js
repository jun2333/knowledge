import{_ as e,o as a,c as s,a4 as i}from"./chunks/framework.BBs01uty.js";const k=JSON.parse('{"title":"阶段 5：安全与权限","description":"","frontmatter":{},"headers":[],"relativePath":"java-practice/05-security-auth.md","filePath":"java-practice/05-security-auth.md"}'),n={name:"java-practice/05-security-auth.md"};function o(r,t,l,d,h,p){return a(),s("div",null,[...t[0]||(t[0]=[i(`<h1 id="阶段-5-安全与权限" tabindex="-1">阶段 5：安全与权限 <a class="header-anchor" href="#阶段-5-安全与权限" aria-label="Permalink to &quot;阶段 5：安全与权限&quot;">​</a></h1><blockquote><p><strong>目标</strong>：搞懂 JWT 无状态鉴权 + Spring Security 过滤器链 + RBAC 动态权限，理解&quot;带 token 的请求&quot;和&quot;不带 token 的请求&quot;分别怎么被处理。</p><p><strong>前置</strong>：阶段 1 完成（已经历过 401）。这是 mall 最值得读的安全部分。</p></blockquote><h2 id="一、验收标准-做完自检" tabindex="-1">一、验收标准（做完自检） <a class="header-anchor" href="#一、验收标准-做完自检" aria-label="Permalink to &quot;一、验收标准（做完自检）&quot;">​</a></h2><ul><li>[ ] 能画出&quot;带 token 请求&quot;经过哪些过滤器，token 在哪个环节被解析、用户信息在哪被塞进上下文</li><li>[ ] 能解释白名单（<code>secure.ignored.urls</code>）为什么能绕过鉴权</li><li>[ ] 能解释 RBAC（用户-角色-资源）在 mall 里怎么实现动态权限</li><li>[ ] 完成动手任务（改白名单观察 401/403 变化）</li></ul><h2 id="二、先建立整体认知" tabindex="-1">二、先建立整体认知 <a class="header-anchor" href="#二、先建立整体认知" aria-label="Permalink to &quot;二、先建立整体认知&quot;">​</a></h2><p>mall 的鉴权方案：<strong>JWT + Spring Security + RBAC</strong>。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">graph TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A[请求进来] --&gt; B{是白名单?&lt;br&gt;secure.ignored.urls}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B -- 是 --&gt; C[直接放行 permitAll]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B -- 否 --&gt; D[JwtAuthenticationTokenFilter&lt;br&gt;从 Header 取 token,解析用户]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    D --&gt; E{token 有效?}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E -- 否 --&gt; F[401 未认证]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E -- 是 --&gt; G[把用户塞进 SecurityContext]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G --&gt; H{DynamicAuthorizationManager&lt;br&gt;检查权限}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    H -- 有权限 --&gt; I[Controller 执行]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    H -- 无权限 --&gt; J[403 无权限]</span></span></code></pre></div><h2 id="三、操作步骤" tabindex="-1">三、操作步骤 <a class="header-anchor" href="#三、操作步骤" aria-label="Permalink to &quot;三、操作步骤&quot;">​</a></h2><h3 id="step-1-读-jwt-工具类" tabindex="-1">Step 1：读 JWT 工具类 <a class="header-anchor" href="#step-1-读-jwt-工具类" aria-label="Permalink to &quot;Step 1：读 JWT 工具类&quot;">​</a></h3><p>打开 <code>mall-security/.../util/JwtTokenUtil.java</code>，重点看三个方法：</p><ul><li><code>generateToken()</code>：把 <strong>用户名 + 权限</strong> 和 <strong>过期时间</strong> 编码进 token（三段式：header.payload.signature）</li><li><code>getUserNameFromToken()</code>：从 token 里解析出用户名（登录时怎么加密，这里就怎么解密）</li><li><code>validateToken()</code>：校验签名 + 过期时间</li></ul><blockquote><p>用 <a href="https://jwt.io" target="_blank" rel="noreferrer">jwt.io</a> 把 mall 返回的 token 粘进去解码，亲眼看 payload 里有什么。</p></blockquote><h3 id="step-2-读过滤器——token-在哪被解析" tabindex="-1">Step 2：读过滤器——token 在哪被解析 <a class="header-anchor" href="#step-2-读过滤器——token-在哪被解析" aria-label="Permalink to &quot;Step 2：读过滤器——token 在哪被解析&quot;">​</a></h3><p>打开 <code>mall-security/.../component/JwtAuthenticationTokenFilter.java</code>（<code>doFilterInternal</code> 方法）：</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. 从 Header 拿 token（默认 Header 名叫 Authorization）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">String authHeader </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getHeader</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.tokenHeader);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;Bearer xxx.xxx.xxx&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. 去掉 &quot;Bearer &quot; 前缀</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">String authToken </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authHeader.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">substring</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.tokenHead.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3. 解析 token 拿用户名，查用户和权限</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 4. 构造 UsernamePasswordAuthenticationToken 塞进 SecurityContextHolder</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 5. 之后 Controller 里 @AuthenticationPrincipal 或 SecurityContextHolder.getContext() 就能拿到当前用户</span></span></code></pre></div><p><strong>关键理解</strong>：这个过滤器<strong>只做解析</strong>，不做拦截。它把用户身份放好，后续&quot;要不要放行&quot;交给安全配置和权限管理器。</p><h3 id="step-3-读安全配置——白名单怎么生效" tabindex="-1">Step 3：读安全配置——白名单怎么生效 <a class="header-anchor" href="#step-3-读安全配置——白名单怎么生效" aria-label="Permalink to &quot;Step 3：读安全配置——白名单怎么生效&quot;">​</a></h3><p>打开 <code>mall-security/.../config/SecurityConfig.java</code>：</p><ul><li><code>ignoredUrlsConfig.getUrls()</code> 读的是 <code>application.yml</code> 里 <code>secure.ignored.urls</code>（就是 <code>/admin/login</code>、<code>/swagger-ui/</code> 那些）</li><li>白名单路径 <code>requestMatchers(url).permitAll()</code> 直接放行</li><li>其余请求：先过 <code>JwtAuthenticationTokenFilter</code>，再被动态权限管理器检查</li></ul><blockquote><p>还记得阶段 1 那个 401 吗？<code>/swagger-ui.html</code> 不在白名单（白名单写的是 <code>/swagger-ui/</code>），所以被拦。去 <code>application.yml</code> 的 <code>secure.ignored.urls</code> 里加一行 <code>/swagger-ui.html</code> 就通了——你现在已经能自己修这个问题了。</p></blockquote><h3 id="step-4-读动态权限——rbac-怎么落地" tabindex="-1">Step 4：读动态权限——RBAC 怎么落地 <a class="header-anchor" href="#step-4-读动态权限——rbac-怎么落地" aria-label="Permalink to &quot;Step 4：读动态权限——RBAC 怎么落地&quot;">​</a></h3><p>打开 <code>mall-security/.../component/DynamicAuthorizationManager.java</code>（<code>check</code> 方法）：</p><ul><li>根据当前请求的 <strong>URL + 请求方法</strong> 找到对应的资源 ID（查 <code>ums_resource</code> 表）</li><li>看当前登录用户的<strong>角色</strong>（用户 → 角色 → 资源，三张表关联）是否有该资源的权限</li><li>有 → 放行；没有 → 403</li></ul><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- RBAC 三张核心表（可去数据库里查数据）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ums_admin          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 用户</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ums_role           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 角色</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ums_resource       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 资源（对应接口 URL）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ums_admin_role_relation     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 用户-角色</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ums_role_resource_relation  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 角色-资源</span></span></code></pre></div><p><strong>动手</strong>：去 MySQL 里查 <code>ums_resource</code> 表，看看接口 URL 是怎么登记为资源的；再查 admin 用户的角色，验证它有全部资源权限。</p><h2 id="四、动手任务" tabindex="-1">四、动手任务 <a class="header-anchor" href="#四、动手任务" aria-label="Permalink to &quot;四、动手任务&quot;">​</a></h2><ol><li><strong>改白名单</strong>：在 <code>application.yml</code> 的 <code>secure.ignored.urls</code> 里加 <code>/admin/info</code>，重启后不登录调 <code>GET /admin/info</code> → 变成 200（白名单生效了）。测试完删掉这行</li><li><strong>体验 403</strong>：在 <code>ums_admin_role_relation</code> 里把 admin 的超级管理员角色换成一个普通角色，重启后用普通角色登录，调一个没有权限的接口 → 观察 403</li><li>在笔记里画出两种请求（带/不带 token）的完整处理流程图</li></ol><h2 id="五、实战调试心得-踩坑实录" tabindex="-1">五、实战调试心得（踩坑实录） <a class="header-anchor" href="#五、实战调试心得-踩坑实录" aria-label="Permalink to &quot;五、实战调试心得（踩坑实录）&quot;">​</a></h2><blockquote><p>以下坑都是真实踩过的，调试安全相关断点直接对照，能省大量时间。</p></blockquote><h3 id="_1-jwt-过滤器只在-请求带-token-时才干活" tabindex="-1">1. JWT 过滤器只在&quot;请求带 token&quot;时才干活 <a class="header-anchor" href="#_1-jwt-过滤器只在-请求带-token-时才干活" aria-label="Permalink to &quot;1. JWT 过滤器只在&quot;请求带 token&quot;时才干活&quot;">​</a></h3><p><code>JwtAuthenticationTokenFilter</code> 第 41 行：</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (authHeader </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authHeader.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">startsWith</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bearer &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) {  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 不带 token 这层直接跳过</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...解析 token 的逻辑...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>坑</strong>：调登录接口（不带 token）时，在 if 内部打断点永远不停——因为条件不成立，解析代码根本不执行。</p><p><strong>解法</strong>：想触发 filter 的解析逻辑，必须调<strong>带 token</strong> 的受保护接口（先登录拿 token → Authorize 填入 → 再调接口）。</p><h3 id="_2-swagger-会-偷偷-带-token" tabindex="-1">2. Swagger 会&quot;偷偷&quot;带 token <a class="header-anchor" href="#_2-swagger-会-偷偷-带-token" aria-label="Permalink to &quot;2. Swagger 会&quot;偷偷&quot;带 token&quot;">​</a></h3><p><strong>坑</strong>：你以为 Swagger 里 Authorize 没值、请求没带 token，但请求实际进了 Controller（WebLogAspect 日志能看出来）——因为 Authorize 里存的旧 token 会自动附加到每个请求头。</p><p><strong>解法</strong>：判断请求到底带没带 token，在 <code>JwtAuthenticationTokenFilter</code> 第 40 行（<code>request.getHeader</code>）打断点看 <code>authHeader</code> 的值，或者用浏览器/curl 发一个绝对干净的请求。</p><h3 id="_3-白名单接口绕过动态权限" tabindex="-1">3. 白名单接口绕过动态权限 <a class="header-anchor" href="#_3-白名单接口绕过动态权限" aria-label="Permalink to &quot;3. 白名单接口绕过动态权限&quot;">​</a></h3><p><strong>坑</strong>：调 <code>/admin/info</code>（它在 <code>secure.ignored.urls</code> 白名单里），在 <code>DynamicAuthorizationManager.check()</code> 打断点永远不停——白名单在<strong>匹配阶段</strong>（<code>requestMatchers().permitAll()</code>）就放行了，请求根本走不到授权阶段。</p><p><strong>解法</strong>：调试动态权限必须用<strong>非白名单</strong>接口（如 <code>GET /admin/list</code>、<code>GET /product/list</code>）。</p><h3 id="_4-浏览器缓存会-吃掉-你的请求" tabindex="-1">4. 浏览器缓存会&quot;吃掉&quot;你的请求 <a class="header-anchor" href="#_4-浏览器缓存会-吃掉-你的请求" aria-label="Permalink to &quot;4. 浏览器缓存会&quot;吃掉&quot;你的请求&quot;">​</a></h3><p><strong>现象</strong>：普通刷新断点不进，<code>Cmd+Shift+R</code> 强刷就进，F12 打开就每次进。</p><p><strong>原因</strong>：浏览器缓存了 GET 响应，普通刷新不重新发请求。</p><p><strong>解法</strong>（三选一）：</p><ul><li>调试时保持 <strong>F12 DevTools</strong> 打开（Network 面板打开时 Chrome 默认禁用缓存）</li><li>请求 URL 加随机参数：<code>/admin/list?t=1700000000000</code></li><li>用 <strong>curl</strong>（默认不缓存）：<code>curl -H &quot;Authorization: Bearer xxx&quot; http://localhost:8080/admin/list</code></li></ul><blockquote><p>面试关联：<code>Cache-Control: no-cache</code>（允许缓存但每次回源验证）vs <code>no-store</code>（彻底不缓存）。</p></blockquote><h3 id="_5-配置期-vs-运行期-配置代码的断点只在启动时停" tabindex="-1">5. 配置期 vs 运行期：配置代码的断点只在启动时停 <a class="header-anchor" href="#_5-配置期-vs-运行期-配置代码的断点只在启动时停" aria-label="Permalink to &quot;5. 配置期 vs 运行期：配置代码的断点只在启动时停&quot;">​</a></h3><p><strong>坑</strong>：在 <code>SecurityConfig.java</code> 第 43 行（<code>requestMatchers(url).permitAll()</code>）打断点，请求时不停。</p><p><strong>原因</strong>：那是<strong>启动时构建过滤器链</strong>的配置代码，只执行一次；请求时的白名单判断由框架内部完成，不执行这段 Java 代码。</p><p><strong>同样</strong>：<code>addFilterBefore</code> 是<strong>声明式</strong>的（&quot;把我的过滤器插在 X 之前&quot;），运行期由框架按规则重排过滤器链——<strong>代码书写顺序 ≠ 运行期执行顺序</strong>。JWT 过滤器虽然写在后，实际一定在授权检查（AuthorizationFilter）之前执行。</p><h3 id="_6-dynamicauthorizationmanager-里的白名单是-死代码" tabindex="-1">6. DynamicAuthorizationManager 里的白名单是&quot;死代码&quot; <a class="header-anchor" href="#_6-dynamicauthorizationmanager-里的白名单是-死代码" aria-label="Permalink to &quot;6. DynamicAuthorizationManager 里的白名单是&quot;死代码&quot;&quot;">​</a></h3><p><code>check()</code> 第 44-53 行（白名单判断 + OPTIONS 放行）和 SecurityConfig 里的白名单配置<strong>逻辑重复</strong>，且正常流程<strong>执行不到</strong>（白名单请求在匹配阶段就放行了）。</p><p>作者写它是<strong>防御性冗余</strong>（防止这个 Manager 被复用到别的过滤器链）。判断死代码的方法见第 7 条。</p><h3 id="_7-核心调试方法-对比实验法" tabindex="-1">7. 核心调试方法：对比实验法 <a class="header-anchor" href="#_7-核心调试方法-对比实验法" aria-label="Permalink to &quot;7. 核心调试方法：对比实验法&quot;">​</a></h3><p><strong>不确定某段代码走不走，就发两个不同条件的请求对比断点触发情况。</strong></p><table tabindex="0"><thead><tr><th>想验证什么</th><th>实验</th><th>结论</th></tr></thead><tbody><tr><td>check() 是否处理白名单</td><td>调 <code>/admin/login</code>（白名单）vs <code>/admin/list</code>（非白名单）</td><td>前者断点不停 = 白名单在更外层放行</td></tr><tr><td>filter 是否处理无 token 请求</td><td>不带 token vs 带 token 调同一接口</td><td>前者 if 不成立 = 解析逻辑只在带 token 时跑</td></tr><tr><td>401 vs 403 分流</td><td>无 token vs 低权限账号带 token 调同一接口</td><td>前者进 EntryPoint，后者进 AccessDeniedHandler</td></tr></tbody></table><h3 id="_8-token-有效性对照表-调试时直接对" tabindex="-1">8. token 有效性对照表（调试时直接对） <a class="header-anchor" href="#_8-token-有效性对照表-调试时直接对" aria-label="Permalink to &quot;8. token 有效性对照表（调试时直接对）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>请求带的情况</th><th>filter 行为</th><th>最终结果</th></tr></thead><tbody><tr><td>无 token</td><td>if 不成立，不解析</td><td>授权拒绝 → <strong>401 EntryPoint</strong></td></tr><tr><td>token 过期</td><td><code>validateToken</code> 失败，不设身份</td><td>授权拒绝 → <strong>401 EntryPoint</strong></td></tr><tr><td>token 无效</td><td><code>getUserNameFromToken</code> 返回 null，不设身份</td><td>授权拒绝 → <strong>401 EntryPoint</strong></td></tr><tr><td>token 有效</td><td>设置身份到 SecurityContextHolder</td><td>授权通过 → <strong>Controller</strong></td></tr></tbody></table><blockquote><p><strong>一句话</strong>：EntryPoint 的触发条件是&quot;请求最终没有有效认证&quot;，与 token 有没有/过没过期无关，只看最后有没有身份。</p></blockquote><h2 id="六、常见问题" tabindex="-1">六、常见问题 <a class="header-anchor" href="#六、常见问题" aria-label="Permalink to &quot;六、常见问题&quot;">​</a></h2><p><strong>Q：JWT 和 Session 的区别？</strong> A：Session 存在服务端（有状态），JWT 存在客户端（无状态，服务端不存）。mall 用 JWT，所以&quot;登录&quot;就是发你一个 token，之后每次都带。</p><p><strong>Q：token 过期了怎么办？</strong> A：mall 提供 <code>/admin/refreshToken</code>（看 <code>UmsAdminController</code>），前端定期刷新。token 本身设计为短期有效。</p><p><strong>Q：白名单是不是不安全？</strong> A：白名单只放行<strong>不需要登录就能访问</strong>的接口（登录、注册、Swagger 文档），业务接口一律不放行。</p><h2 id="七、关联理论" tabindex="-1">七、关联理论 <a class="header-anchor" href="#七、关联理论" aria-label="Permalink to &quot;七、关联理论&quot;">​</a></h2><ul><li><a href="/knowledge/service/auth.html">认证与授权</a> — JWT/Session/OAuth 概念</li><li><a href="/knowledge/service/permission-design.html">权限设计</a> — RBAC 模型</li><li><a href="/knowledge/service/spring-principles.html">Spring 核心原理</a> — Spring Security 过滤器链、AOP</li></ul>`,65)])])}const g=e(n,[["render",o]]);export{k as __pageData,g as default};
