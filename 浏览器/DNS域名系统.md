### DNS 递归查询与迭代查询
DNS 递归查询是浏览器到本地 DNS 的过程，
DNS 迭代查询是本地 DNS 服务器到根域名服务器查询的方式。（本地 DNS：所在区域或城市的 DNS 服务器）

### 查询流程
1. 浏览器缓存
2. 操作系统host配置
3. 路由器缓存
4. 本地DNS服务器(后面的步骤类似于踢皮球)
5. 根DNS服务器: 查不到则返回顶级域名
6. 查询上一步返回的顶级域名，顶级域名查不到则返回主DNS域名服务器
7. 查询上一步返回的主域名服务器，主域名返回下一级域名，依次迭代，直到查到为止