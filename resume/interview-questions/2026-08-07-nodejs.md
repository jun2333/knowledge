# 后端专项模拟面试题（Node.js）- 2026.08.07

> 面向全栈 / 后端转岗方向，以 Node.js 为核心展开，覆盖核心机制、Web 框架、数据库、鉴权安全、部署运维、性能高并发、综合设计、手写题八个方向。
> 全部为后端面试高频经典题，偏向实战。
> 每道题下方留有答题区域，建议先独立作答再对照资料查漏补缺。

---

## 一、Node.js 核心机制（8 题）

### 1.1 事件循环机制

> 1. Node.js 的事件循环有哪些阶段（phase）？各阶段的作用？
> 2. `process.nextTick` 和 `setImmediate` 的区别？
> 3. 以下代码的输出顺序是什么？为什么？
>
> ```js
> setTimeout(() => console.log('timeout'), 0);
> setImmediate(() => console.log('immediate'));
> process.nextTick(() => console.log('nextTick'));
> Promise.resolve().then(() => console.log('promise'));
> console.log('sync');
> ```

**你的回答：**
1. timer(检查定时器回调是否到期)->pending callback(推迟到下一轮的I/O回调)->poll(轮询I/O回调)->check(检查setImmediate)->close(执行关闭事件回调)
2. `process.nextTick`优先级比微任务高，它在每个回调执行之间执行，而`setImmediate`是在事件循环check阶段检查执行
3. sync、nextTick、promise、immediate、timeout
同步代码最先执行，微任务中nextTick优先级最高，它先于Promise等微任务，随后是宏任务：immediate之所以先于timeout是因为一般定时器任务在首轮事件循环即使判断到期，但此时存在setImmediate回调，也会先执行完setImmediate，然后下一轮才执行timer回调

---

---

**批改（2026-09-08）：1.1 事件循环机制 6/10**

> 第 1 问 ✅ 阶段顺序答对(timers → pending → poll → check → close)。可补两点：阶段间还有内部阶段 idle/prepare；**poll 阶段没有到期回调时会等待 I/O 事件**。
> 第 2 问 ⚠️ 有一处概念混淆：`nextTick` **本身就是微任务队列**，不是"比微任务高"——准确说法是它在**当前操作结束后、Promise 微任务之前**执行（同属 microtask，但排在 Promise.then 前面）；`setImmediate` 是宏任务，在 check 阶段执行。优先级：nextTick > Promise > setImmediate。
> 第 3 问 ⚠️ **后半段说反了**。准确结论：
>
> ```js
> // 输出:
> sync        // 同步代码
> nextTick    // 微任务,先于 promise
> promise     // Promise 微任务
> timeout 或 immediate   // 两者顺序【不确定】!
> ```
>
> 原因：`setTimeout(0)` 的回调进 timers 队列，真正到期大约在启动后 ~1ms；`setImmediate` 进当前循环的 check 队列。事件循环第一轮**先进 timers 阶段**：若定时器此刻已到期就先执行 timeout，否则这一轮先到 check 执行 immediate、下一轮 timers 再执行 timeout——**两者竞争，主模块顶层结果不定**。
> **唯一确定的情况**：把两者放进一个 I/O 回调（如 fs.readFile 回调）里，`immediate` **恒先于** timeout（I/O 回调在 poll 阶段执行，poll 之后紧跟 check）。
> 记忆锚点：`nextTick` = 每次操作结束后、同轮内最早；`setImmediate` = 本轮 check 阶段；两者都不等于"立即执行"。

---

### 1.2 单线程与高并发

> 1. 为什么说 Node.js 是单线程的？它如何支撑高并发？
> 2. 事件驱动 + 非阻塞 I/O 的工作机制是什么？（libuv 线程池、IO 多路复用）
> 3. Node.js 的单线程瓶颈在哪？（CPU 密集任务）什么时候需要 worker_threads / cluster？

**你的回答：**
1. 因为Node.js基于V8引擎设计，只有一个线程执行js主脚本，所以是单线程；支撑高并发靠的是异步I/O和事件循环机制，异步I/O不阻塞代码执行，其回调会在事件循环中有序执行
2. 非阻塞I/O靠的是多路复用+事件循环机制，充分利用线程的能力；而事件驱动靠的是libuv线程池，libuv负责主线程与其他线程之间的通信
3. 涉及复杂的计算类的CPU密集型任务如果单靠主线程会影响性能，因此需要worker_threads多线程，将CPU密集型任务放到另外线程去计算，它与主线程内存共享可通过postMessage通信；cluster是多进程集群，它可以充分发挥CPU的多核性能，并发启动多个工作进程，由主进程统一进行分发请求，即负载均衡

---

---

**批改（2026-09-08）：1.2 单线程与高并发 6.5/10**

> 第 1 问 ✅ 方向对，补精确表述：单线程指 **JS 执行引擎只有一个线程**跑业务代码；支撑高并发不是"靠更多线程"，而是**少量线程 + 事件循环 + 非阻塞 I/O**，线程几乎不被阻塞，单线程也能同时服务大量连接。
> 第 2 问 ⚠️ 概念有点混，理清分工：
> - **网络 I/O**（socket）走系统 **I/O 多路复用**（epoll/kqueue），不需要线程池；
> - **libuv 线程池**（默认 4 个线程）只处理内核没有异步接口的阻塞操作：**文件系统 I/O、DNS 解析、部分 crypto、zlib**；
> - libuv 不是"通信员"，它是**事件循环的实现者**：把异步操作完成后的回调放回主线程依次执行。
> 第 3 问 ✅ 基本对。小修正：`worker_threads` 各线程**默认不共享内存**，靠 postMessage 传消息；真要共享才用 SharedArrayBuffer/transferable。cluster = 多进程（内存隔离）+ 主进程分发。

---

### 1.3 CommonJS 与 ESM

> 1. CommonJS 和 ES Module 的区别？
> 2. `require` 的加载过程？（路径解析、模块缓存、循环依赖）
> 3. 为什么 ESM 可以做 tree-shaking 而 CommonJS 不行？

**你的回答：**
1. CommonJS是基于闭包封装的运行时模块化方案，而ES Module是ES6以后官方支持的模块化方案，它在编译过程已经做好了模块划分，性能上优于CMS
2. 原理的细节说不清楚
3. 因为ESM在编译阶段已经做好了模块划分，每个模块内部用到的变量或者函数都被引用了，未引用的即可删除；而CMS需要运行时才能生成模块，这时候也没法删掉没用代码了(程序都在跑了)

---

---

**批改（2026-09-08）：1.3 CommonJS 与 ESM 7/10**

> 第 1 问 ✅ 方向对，补全：CMJ 是**运行时**模块（require 是函数、模块文件被包装成 `(function(exports, require, module, __filename, __dirname){...})` 执行）；ESM 是**编译期静态**模块（import/export 是语法，模块关系先于执行确定），支持静态分析优化（tree-shaking）、异步加载。CMJ 同步 require、ESM 支持顶层 await。
> 第 2 问（未答）参考：
> 1. **路径解析**：① 内置核心模块（fs、path 直接返回）→ ② 相对/绝对路径 → ③ `node_modules` 从当前目录逐级向上找；
> 2. **扩展名补全**：依次尝试 `.js` `.json` `.node`；目录则读 `package.json` 的 `main`，没有则取 `index.js`；
> 3. **加载执行**：包装成函数、注入 exports/require/module 执行；
> 4. **缓存**：模块执行后缓存进 `require.cache`，再次 require 直接返回缓存的 `module.exports`；
> 5. **循环依赖**：A→B→A 时，B 里 require 到的 A 是"加载到一半的 A"，只拿到 A **已执行完导出的部分**（其余是 undefined）。
> 第 3 问 ✅ 理解对：ESM 导入导出是语法、可静态分析 → 未用导出可安全剔除；CMJ 的 require 是运行时函数调用（甚至可以 `if(x) require(y)` 动态拼路径）→ 编译器无法静态判断"谁被用到"，只能全保留。

---

### 1.4 Buffer

> 1. Buffer 是什么？为什么需要 Buffer？
> 2. Buffer 和普通数组、TypedArray 的关系？
> 3. 如何将 Buffer 转字符串？有哪些编码？（utf8 / base64 / hex）

**你的回答：**
1. Buffer是处理二进制数据的核心类型，为什么需要Buffer我也说不好原因
2. 不知道，TypedArray是啥我也不知道
3. Buffer.from().toString()；from内容，toString传编码类型

---

---

**批改（2026-09-08）：1.4 Buffer 4/10**

> 第 1 问 ⚠️ 知道 Buffer 是二进制类型，但"为什么需要"没说清。补关键点：JS 字符串是 **UTF-16 编码**，不适合也不能高效处理二进制数据（文件、网络包、图片、加密结果都是字节流）；Buffer = **V8 堆外分配的原始内存**，读写不走 GC 管理的字符串。
> 第 2 问（未答，且 TypedArray 是常见盲区）参考：
> - **TypedArray**：ES6 标准的一组二进制数组类型（Uint8Array、Int16Array...），本质是 ArrayBuffer 的"类型化视图"；
> - **Buffer = Uint8Array 的子类**（Node 专用扩展），额外提供 utf8/base64/hex 等编码转换、池化分配、`Buffer.from/alloc` 等 API；
> - 和普通数组区别：数组存任意 JS 值、长度可变、有 push/splice；Buffer 存 0-255 整数、固定长度、性能远超普通数组。
> 第 3 问 ⚠️ 用法对但没答全。参考：
>
> ```js
> // 字符串 → Buffer
> Buffer.from("你好", "utf8");       // utf8 默认
> Buffer.from("aGk=", "base64");     // 解码 base64
> // Buffer → 字符串
> buf.toString("utf8");              // 默认 utf8
> buf.toString("hex");               // 16 进制
> buf.toString("base64");
> // 常见编码:utf8 / utf16le / latin1 / base64 / hex / ascii
> ```
>
> 记忆锚点：**数据进 Node 变 Buffer（二进制世界），要给人/日志看再 toString 成字符串**。

---

### 1.5 Stream 流

> 1. Stream 是什么？有哪些类型（Readable / Writable / Duplex / Transform）？
> 2. 为什么读取大文件要用流而不是 `fs.readFile`？
> 3. `pipe` 的原理？什么是背压（backpressure）？

**你的回答：**
1. 专门处理连续数据的核心抽象，Readable(可读流) / Writable(可写流) / Duplex(双工流) / Transform(转换流)
2. 大文件用流分块处理不用占用太多内存，它就像水流一样流过内存，用fs读大文件占用大量内存
3. 不知道

---

---

**批改（2026-09-08）：1.5 Stream 流 5.5/10**

> 第 1 问 ✅ 四类答对，各补一句场景：Readable 读数据源（文件、网络）；Writable 写目的地；Duplex 双工（net.Socket）；Transform 转换（zlib 压缩、crypto 加密——读的同时写、数据被改）。
> 第 2 问 ✅ 对：readFile 一次把整个文件读进内存；流按 chunk（默认 64KB）分块处理，**内存占用与文件大小无关**。补充：readFile 需等整份文件就绪才能开始处理（首字节延迟高），流可边读边处理。
> 第 3 问（未答）参考：
> - **pipe 原理**：`readable.pipe(writable)` 内部自动 `data` 事件读一块写一块，处理 error/end 并暂停/恢复数据流。
> - **背压（backpressure）**：生产速度 > 消费速度时内部缓冲暴涨会撑爆内存。`writable.write(chunk)` 返回 **false** 表示"写不下了"，应 `readable.pause()` 暂停读；等 writable 触发 **drain**（缓冲排空）再 resume。`pipe` 自动做了这套，所以流式传大文件不爆内存。
> - 手动流推荐 `pipeline()`（stream/promises），比 pipe 多了错误传播与自动清理，支持多段链。示例：
>
> ```js
> import { pipeline } from "node:stream/promises";
> import { createReadStream, createWriteStream } from "node:fs";
> import { createGzip } from "node:zlib";
>
> // 一段链：读文件 → gzip 压缩 → 写文件,任一段出错会自动 reject
> await pipeline(
>   createReadStream("source.txt"),
>   createGzip(),
>   createWriteStream("source.txt.gz"),
> );
>
> // 对比 pipe 的坑:
> // rs.pipe(gzip).pipe(ws) 链中某段出错时,错误不会自动向整条链传播,
> // 残留的流不会自动销毁,可能悬空(文件句柄/资源泄漏)。
> // pipeline 统一处理:错误传播到第一个流并销毁整条链。
> ```
>
> 记忆锚点：**管道中间要压缩/加密/转换 → 用 Transform 接在 pipeline 里；`pipe` 能跑但生产代码一律用 `pipeline`**。

---

### 1.6 错误处理

> 1. Node.js 中如何正确捕获异步错误？（callback 错误优先、Promise、async/await）
> 2. `uncaughtException` / `unhandledRejection` 是什么？发生之后进程会怎样？
> 3. 生产环境遇到未捕获异常应该怎么处理？（记录日志 + 自动重启）

**你的回答：**
1. 使用事件监听全局异步错误，具体异步操作用try...catch或者Promise.catch，回调写法则在回调函数里捕获错误
2. 未捕获的异常/未捕获的异步错误，发生之后进程会挂掉
3. 监听未捕获异常进行日志记录，并且多进程集群管理自动重启，如PM2

---

---

**批改（2026-09-08）：1.6 错误处理 6/10**

> 第 1 问 ⚠️ 答得泛且次序反了。正确分层：
> 1. **同步代码** → try/catch；
> 2. **async/await** → try/catch；Promise 链 → `.catch()`；
> 3. **callback 风格** → 错误优先参数 `(err, data)`，回调先判 `if (err)`；EventEmitter 风格（http server）→ 监听 `error` 事件；
> 4. **全局兜底**（最后手段，非主流程）：`uncaughtException` / `unhandledRejection` 监听记录日志后退出重启。**不能用全局监听替代局部捕获**。
> 第 2 问 ✅ 结论对，补细节：`uncaughtException` 默认打印堆栈退出进程；`unhandledRejection` 在 Node 15+ 默认直接抛错退出（更早只警告）。
> 第 3 问 ✅ 对：生产标准做法是"进程内兜底只记录日志 → 主动退出 → 由 PM2/Docker/K8s 自动重启"，而非捕获后继续跑（此时状态可能不一致，更危险）。

---

### 1.7 模块系统深入

> 1. `require` 查找模块的路径解析规则是怎样的？
> 2. Node.js 模块缓存机制？如何强制清除缓存？
> 3. 循环依赖会发生什么？如何避免？

**你的回答：**
1. 有三种路径，绝对路径、相对路径以及其他；对于其他这类的路径解析规则是先看项目内的自定义别名，然后看npm包描述文件定义去node_modules里查找模块，找不到则往上一级目录查找node_modules，以此类推
2. 不知道
3. 不知道

---

---

**批改（2026-09-08）：1.7 模块系统深入 4/10**

> 第 1 问 ⚠️ 答的基本是打包器（vite/webpack）的解析，不是 Node 原生。Node 的 `require` 解析顺序：
> 1. **核心模块**（fs、path、http——内置优先，无需查找）；
> 2. **以 `/`、`./`、`../` 开头** → 绝对/相对路径；
> 3. **裸模块名** → 从当前目录逐级向上找 `node_modules/<name>`，直到根目录；
> 4. 找到后**补扩展名**：依次试 `.js` `.json` `.node`；是目录则读 `package.json` 的 `main`，没有取 `index.js`。
> （"自定义别名"是构建工具 paths 配置，不是 Node 行为——别混）
> 第 2、3 问（未答）参考：
> - **缓存**：首次 require 后缓存于 `require.cache`（key 为解析后绝对路径）。清除：`delete require.cache[require.resolve("./foo.js")]`。
> - **循环依赖**：A require B、B 又 require A 时，Node 用"半成品导出"：B 拿到的是 A 当前已执行完的部分，未执行的导出是 undefined。避免：设计上不成环；把共享部分抽成第三模块；或在函数体内延迟 require（运行时取值）。ESM 对循环处理更强（live binding），仍应避免。
> 建议：把 1.3.2 与 1.7.2/1.7.3 合并成一张"require 完整流程"图记忆。

---

### 1.8 进程与线程

> 1. `child_process` 和 `worker_threads` 的区别？
> 2. 进程间通信（IPC）有哪些方式？
> 3. cluster 模式的原理？（主进程 + 工作进程、端口共享）

**你的回答：**

1. `child_process`是子进程，与主线程内存不互通，用于批处理命令或者脚本执行，进程间完全隔离；`worker_threads`是多线程，与主线程内存互通可通过postMessage通信，通常用于CPU密集型任务
2. 不知道
3. 以多进程方式启动服务，各个进程之间共享端口，主进程负责分发请求、负载均衡，工作进程处理具体的请求，充分发挥CPU多核能力
---

---

**批改（2026-09-08）：1.8 进程与线程 7/10**

> 第 1 问 ✅ 基本对。补精确对比：
>
> | | child_process | worker_threads |
> |---|---|---|
> | 本质 | **进程**（独立 PID、独立 V8、内存隔离） | **线程**（同进程内） |
> | 内存 | 完全隔离 | 默认隔离，postMessage 传数据；可共享 SharedArrayBuffer |
> | 通信 | IPC 通道 / stdio 管道 | postMessage（更轻） |
> | 用途 | 执行外部命令、隔离不稳定代码 | CPU 密集（计算、图像）、需共享进程内资源 |
> | 开销 | 较重（整进程） | 较轻 |
>
> 第 2 问（未答）参考——IPC 方式：**同机**：管道、信号、Unix socket、共享内存、消息队列、`child_process` 自带 IPC channel（fork + send）；**跨机**：TCP/HTTP、消息队列中间件（RabbitMQ/Kafka）、gRPC。Node 最常见答案 = fork + IPC channel、Redis 等中间件。
> 第 3 问 ✅ 对，补细节：cluster 下**主进程监听端口**并把连接分发给 worker（Linux 默认 round-robin）；每个 worker 独立进程，崩溃不影响其它 worker；`pm2 start app.js -i max` 即可起 cluster。

---

## 二、Web 框架与中间件（6 题）

### 2.1 Express vs Koa

> 1. Express 和 Koa 的主要区别？
> 2. 为什么说 Koa 的洋葱模型更利于写中间件？（async/await 支持）
> 3. 你更推荐哪个？为什么？（可结合你项目中使用的 Koa）

**你的回答：**
1. Express不是洋葱模型，而是线型中间件模型，并且配套比较齐全；而Koa的中间件是洋葱模型
2. 因为控制的更细，就像spring boot的切面技术一样，能用一个中间件将进入和退出的逻辑都涵盖
3. 我推荐Express，因为目前还是主流，并且开箱即用，包括nest.js底层都是基于Express

---

---

**批改（2026-09-08）：2.1 Express vs Koa 6.5/10**

> 第 1 问 ⚠️ 结论对，但"Express 不是洋葱是线性"的措辞会被追问。更准确：
> - **Koa**：中间件是 async 函数，`await next()` 天然支持"进入后再回来" → 完整洋葱；
> - **Express**：中间件本质是回调串，`next()` 只负责往下推，想在 next 后写回程逻辑很别扭 → 习惯称**线性/串行**（理论也能实现洋葱，但非一等公民）。
> 另可补：Express 自带路由模板、生态全；Koa 内核极简（洋葱 + ctx），功能靠中间件拼。
> 第 2 问 ✅ 对，补例：日志/计时/错误处理都能包住后续：
>
> ```js
> async function logger(ctx, next) {
>   const start = Date.now();
>   await next();                    // 请求进去……
>   console.log(ctx.status, Date.now() - start);  // 响应回来再记耗时
> }
> ```
>
> 第 3 问 ⚠️ "推荐 Express 但自己项目用 Koa"会被追问。建议：**按场景选**——要内置全家桶/团队熟悉选 Express；要轻量、组合中间件、掌控流程（你项目用 Koa 正是这类）选 Koa。结合自己项目的理由比"主流"有说服力。

---

### 2.2 洋葱模型

> 1. 什么是洋葱模型？中间件的执行顺序是怎样的？
> 2. 中间件里不调用 `next()` 会发生什么？（响应挂起）
> 3. 结合你的知识库项目，画一下你项目的中间件链路（logger → error → router 等）。

**你的回答：**
1. 每个中间件就像洋葱内部结构的一层，请求进入模型，从外到内经过每一层中间件，再由内到外继续经过每一层中间件，最后返回；中间件之间的执行顺序是按照书写顺序执行，中间件内部执行顺序是先执行await next()前的代码，遇到next则跳下一个中间件，当最后一个中间件的next之后，会执行它next后的代码，然后回到上一个中间件的next后的代码，直到回到首个中间件next后的代码，最后退出洋葱模型
2. 响应一直挂起直至请求超时，next必须主动调用，否则执行异常
3. cors->bodyParser->routes

---

---

**批改（2026-09-08）：2.2 洋葱模型 8.5/10**

> 第 1 问 ✅ 非常好——进入顺序、next 前后代码、由外到内再由内到外全对。可补一句"每层执行两次（进入时 + 回程时），响应是逆序组装的"。
> 第 2 问 ⚠️ 小错：Koa 里中间件链走完没人写 `ctx.body` **默认返回 404**，不是一直挂起。真"挂起超时"是某中间件 `await next()` 后 promise 永不结束（忘 await、事件不触发）。核心说对了：**不调 next = 后续中间件不执行**。
> 第 3 问 ⚠️ 缺 error 与 logger 且顺序不对。知识库项目大致链路：
>
> ```js
> app.use(errorHandler);    // 最外层:兜底一切异常
> app.use(logger);          // 计时 + 记录
> app.use(cors());
> app.use(bodyParser());
> app.use(router.routes()); // 业务
> ```
>
> **错误处理必须最先注册（最外层）**才能接住所有后续抛错；logger 靠外才能记到整个周期。

---

### 2.3 常用中间件

> 1. 如何实现一个请求日志中间件？（记录 method、path、耗时、状态码）
> 2. 如何实现一个全局错误处理中间件？
> 3. body 解析中间件要注意什么？（大小限制、Content-Type 校验）

**你的回答：**
1. 
```javascript
async function log(ctx, next) {
  const start = Date.now();
  await next();  // 执行下一个中间件
  const ms = Date.now() - start;
  console.log(`${ctx.status}-${ctx.method} ${ctx.url} - ${ms}ms`);
}
```
2. 中间件实现如下，作为第一个中间件使用
```javascript
async function handleError(ctx, next) {
  try {
    await next()
  } catch(err){
    // 记录错误日志
  }
}
```
3. 一般都用bodyparser，具体注意啥我也不知道
---

---

**批改（2026-09-08）：2.3 常用中间件 6/10**

> 第 1 问 ✅ 日志中间件正确（计时 + 状态码 + 耗时）。可优化：换真实 logger、记响应大小、慢请求告警。
> 第 2 问 ⚠️ **半成品**：catch 里只记日志没设响应 → 请求 404 或挂住。完整版要记日志 + 设错误响应 + 不暴露内部堆栈：
>
> ```js
> app.use(async (ctx, next) => {
>   try {
>     await next();
>   } catch (err) {
>     ctx.log.error(err);                       // 记完整错误(含堆栈)
>     ctx.status = err.status ?? 500;
>     ctx.body = {
>       code: err.code ?? "INTERNAL_ERROR",
>       message: ctx.status === 500 ? "服务器开小差了" : err.message, // 不给客户端内部细节
>     };
>   }
> });
> ```
>
> 第 3 问（未答）参考——bodyParser 注意：① **大小限制** `{ limit: '1mb' }`（防超大 body）；② **Content-Type 校验**（只解析 json/urlencoded，其它 415）；③ 畸形 JSON 捕获返回 400；④ multipart 上传要 koa-body/multer，同样限大小；⑤ 防 `__proto__` 原型污染。

---

### 2.4 请求生命周期

> 1. 一次 HTTP 请求在 Koa 中的完整生命周期是怎样的？
> 2. 如何获取客户端 IP、User-Agent？如何获取请求体？
> 3. `ctx` 上常用的属性和方法？（ctx.body、ctx.query、ctx.params、ctx.state）

**你的回答：**

---

---

**批改（2026-09-08）：2.4 请求生命周期（未答，已补参考）**

> 1. **完整生命周期**：请求到达 → Koa 建 ctx → 洋葱最外层进入（logger 计时、error 兜底）→ 逐层 `await next()` 深入 → koa-router 匹配路由 → handler 写 `ctx.body` → 逆洋葱回程（各层 next 后代码执行）→ 框架发响应。**注意 `ctx.body` 赋值不是立即发送**，真正响应在整条链走完后生成。
> 2. **取信息**：IP `ctx.ip`（反代后读 X-Forwarded-For，需 `app.proxy = true`）；UA `ctx.get("User-Agent")`；请求体先用 bodyParser 再 `ctx.request.body`。
> 3. **ctx 常用**：
>
> ```js
> ctx.body / ctx.status              // 响应体 / 状态码
> ctx.method / ctx.url / ctx.path    // 请求描述
> ctx.query / ctx.params             // GET query / 路由参数(koa-router)
> ctx.state                          // 跨中间件共享(放 user、requestId)
> ctx.set(name, val) / ctx.get(name) // 响应头 / 请求头
> ctx.throw(401, "未登录")           // 抛错交错误中间件
> ctx.request.body / ctx.request.ip / ctx.cookies / ctx.files
> ```
>
> 建议把知识库项目的中间件 → 路由 → 响应链路亲手画一遍，面试手写很加分。

---

### 2.5 鉴权中间件

> 1. 如何实现一个 JWT 鉴权中间件？（解析、校验、过期处理）
> 2. 白名单路由如何设计？（如登录、注册接口不需要鉴权）
> 3. 如何把用户信息注入后续业务逻辑？（ctx.state.user）

**你的回答：**

---

---

**批改（2026-09-08）：2.5 鉴权中间件（未答，已补参考）**

> 参考实现（jsonwebtoken）：
>
> ```js
> const WHITE_LIST = ["/api/login", "/api/register", "/health"];
>
> app.use(async (ctx, next) => {
>   if (WHITE_LIST.includes(ctx.path)) return next();   // 白名单放行
>
>   const auth = ctx.get("Authorization");              // "Bearer xxx"
>   const token = auth && auth.startsWith("Bearer ") ? auth.slice(7) : null;
>   if (!token) ctx.throw(401, "未登录");
>
>   try {
>     const payload = jwt.verify(token, SECRET);        // 过期自动抛 TokenExpiredError
>     ctx.state.user = payload;                         // 注入后续业务
>     await next();
>   } catch (err) {
>     ctx.throw(401, err.name === "TokenExpiredError" ? "登录已过期" : "无效的 token");
>   }
> });
>
> router.get("/me", (ctx) => { ctx.body = ctx.state.user; });
> ```
>
> 关键点：① 白名单别只 `===` 精确匹配，前缀型路径用 startsWith/正则；② token 放 header（放 query/body 会进日志）；③ 用 `ctx.state.user` 传用户是 Koa 约定，业务统一从 state 取。

---

### 2.6 参数校验

> 1. 为什么服务端必须做参数校验？只做客户端校验行不行？
> 2. 常用的校验方案？（手写、joi、zod）
> 3. 如何统一返回校验错误信息？（错误码 + 字段级提示）

**你的回答：**
1. 因为请求可以绕过客户端发起，所以服务端必须校验，只做客户端校验不行
2. 手写校验逻辑，最好用zod这样可以用schema表达规则，zod帮你校验的，这样更专注业务逻辑
3. 统一规范状态码，错误code以及友好的message，如果需要字段级提示可以加个field

---

---

**批改（2026-09-08）：2.6 参数校验 8/10**

> 第 1 问 ✅ 对。补面试表达：客户端校验只是**体验优化**，服务端校验才是**安全底线**——请求可被 curl/脚本绕过，服务端必须假设输入不可信。
> 第 2 问 ✅ 对。可补：zod/joi 用声明式 schema 描述规则、自动产出错误；zod 还能 `z.infer` 推导 TS 类型（schema 单一真源）；joi Node 生态老牌，zod 在 TS 项目更流行。
> 第 3 问 ✅ 思路对。把"错误码 + 字段级提示"落成约定：
>
> ```js
> {
>   code: 400001,             // 业务错误码(非 HTTP 状态)
>   message: "参数校验失败",   // 通用描述
>   errors: [                 // 字段级明细
>     { field: "email", message: "邮箱格式不正确" },
>     { field: "password", message: "密码至少 8 位" },
>   ],
> }
> ```
>
> 原则：HTTP 状态码给粗粒度（4xx/5xx），业务 code 给细粒度（前端分支判断），errors 给用户精确提示。

---

## 三、数据库（8 题）

### 3.1 MySQL vs MongoDB

> 1. 关系型和非关系型数据库的区别？
> 2. 什么场景选 MySQL，什么场景选 MongoDB？
> 3. 你项目中的选型？（包括 Chroma 向量数据库，说明各自承担什么职责）

**你的回答：**
1. 主要区别还是在于关系型数据库拥有字段级的存储和表关系建立的能力
2. 业务各模块之间有复杂的关联关系选MySQL，简单存在数据用MongoDB
3. 关系型数据库承担主要且复杂业务数据存储；MongoDB负责存储一些未来可能发生变化的业务的业务数据；redis承担缓存服务以及分布式锁的能力；Chroma这样的向量数据库用于存储向量数据

---

### 3.2 索引原理

> 1. 索引是什么？为什么能加快查询？
> 2. B+ 树索引的特点？聚簇索引和非聚簇索引的区别？
> 3. 哪些场景索引会失效？（like '%xx'、函数运算、隐式类型转换、破坏最左前缀）

**你的回答：**

---

### 3.3 联合索引

> 1. 联合索引的最左前缀原则是什么？
> 2. 有索引 (a, b, c)，以下哪些查询能用上索引？
>    - `WHERE a = 1 AND b = 2 AND c = 3`
>    - `WHERE b = 2 AND c = 3`
>    - `WHERE a = 1 AND c = 3`
> 3. 如何根据业务查询频率设计联合索引？（高频等值列放左边，范围列放最后）

**你的回答：**

---

### 3.4 事务与隔离级别

> 1. 事务的 ACID 特性是什么？
> 2. 四种隔离级别？脏读、不可重复读、幻读分别在哪一级被解决？
> 3. 你项目里有用过事务吗？什么场景？（如转账、订单 + 库存）

**你的回答：**

---

### 3.5 乐观锁 vs 悲观锁

> 1. 乐观锁和悲观锁的区别？各自的实现方式？
> 2. 什么场景用哪个？（高并发扣库存）
> 3. 如何使用版本号实现乐观锁？

**你的回答：**
1. 都是用于并发场景操作同一份数据，乐观锁悲观锁的区别简单说就是乐观锁是假设不会有并发操作冲突，直接先去操作了，如果出现冲突也返回操作识别，而悲观锁则假设肯定有操作冲突，操作之前先把数据锁起来，这样别的请求进来也操作不了
乐观锁实现方式通常有：原子sql、表统一加version字段，操作前查一次version，操作时用version条件更新，同样的思路也可以用更新时间等字段，本质上是两次sql，提前查一次然后条件更新
悲观锁通常伴随着事务，在事务中自动开启
2. 下单支付等原子性的操作用悲观锁；抢单场景高并发扣库存用乐观锁
3. 每个表加version字段，更新前查一次version，更新时用之前的version作为条件update

---

### 3.6 Redis 数据类型

> 1. Redis 常用数据类型有哪些？（string / hash / list / set / zset）
> 2. 各自典型应用场景？（缓存、点赞、排行榜、限流、分布式锁）
> 3. Redis 为什么快？（内存、单线程、IO 多路复用）

**你的回答：**
1. String/Hash/List/Set/ZSet
2. string用于存储json用于缓存；Hash用于存储哈希表，存储对象数据；Set可用于去重、抽奖等场景；ZSet可用排行榜；List可用于消息队列限流等场景;此外redis不仅能存数据还可以用于分布式锁(不存在key才设置,存在代表锁了返回失败)
3. 主要是数据存储在内存里，单线程查存，并且IO多路复用

---

### 3.7 缓存三大问题

> 1. 缓存穿透、缓存击穿、缓存雪崩分别是什么？
> 2. 各自的解决方案？（布隆过滤器、互斥锁、随机过期时间）
> 3. 缓存和数据库的一致性如何保证？（先更新数据库还是先删缓存？为什么）

**你的回答：**
1. 缓存穿透是使用数据库不存在的id去穿透缓存，击垮数据库；缓存击穿是热门key失效，大量的请求直接请求数据库，并发重建缓存；缓存雪崩是一些key同时失效导致一瞬间大量请求同时打到数据库
2. 缓存穿透可用未查到也缓存null或者布隆过滤器等手段；缓存击穿可用互斥锁，一个线程重建缓存，其他等着拿缓存或者逻辑过期，牺牲一致性换取性能；缓存雪崩则可采取下面解决措施：
a.过期时间加随机值
b.热门key永不过期(逻辑过期)
c.高可用redis(多节点集群)
d.限流，熔断保护DB
---

### 3.8 分页与查询优化

> 1. 深分页问题是什么？（`LIMIT 100000, 20` 为什么慢）
> 2. 如何优化？（游标分页、延迟关联）
> 3. 慢查询如何排查？（慢查询日志、EXPLAIN）

**你的回答：**

---

## 四、鉴权与安全（6 题）

### 4.1 Session vs JWT

> 1. Session 和 JWT 的区别？
> 2. 各自的优缺点？
> 3. 为什么现在流行 JWT？它有什么坑？

**你的回答：**
1. Session是以cookie的形式存储，服务器可自行管理，因此可主动登出；而JWT是一次性生成发放给客户端之后就无法管理了(由客户端自行存储)，只能等其自动失效，通常客户端主动保存并把它放在请求头中传输，因此也叫无状态鉴权，服务端无法登出。JWT能有效的防CSRF攻击
2. Session的优点就是实现简单方便，服务端能主动失效，缺点是容易被CSRF攻击；JWT优点是更安全(防CSRF)，但无状态，无法主动失效且实现上稍微复杂点
3. 防CSRF攻击，无状态跨客户端使用，一次生成多端使用；坑就是无法主动失效，且payload不加密因此不要存关键信息

---

### 4.2 JWT 详解

> 1. JWT 的组成结构？（header.payload.signature）
> 2. JWT 默认是加密的吗？为什么不能把敏感信息放 payload？
> 3. 刷新令牌（refresh token）机制是什么？为什么需要它？

**你的回答：**
1. header(加密算法+类型)+payload(内容载荷，一般存储用户信息)+signature(签名信息)
2. header和payload是base64编码(非加密)，signature是哈希加密；因此payload没有被加密，所以不安全
3. 给token续期：重新生成Token；因此token会设置exp过期时间，不刷新就会随着过期时间到达自动失效

---

### 4.3 密码安全

> 1. 密码应该如何安全存储？（绝不存明文）
> 2. bcrypt 加盐哈希的原理？盐（salt）的作用？
> 3. 如何防暴力破解？（限流、失败锁定、双因素认证）

**你的回答：**
1. 加密存储，最好不可逆
2. 不知道
3. 限流，失败锁定以及双因素认证

---

### 4.4 OAuth2

> 1. OAuth2.0 是什么？解决什么问题？
> 2. 授权码模式（authorization code）的流程？
> 3. OAuth2 和 JWT 的关系？（OAuth 是授权协议，JWT 是令牌格式）

**你的回答：**
1. 第三方授权的标准，可以让第三方应用在不需要密码前提下得到另一方的授权，从而访问它的资源
2. 第三方登录->获取auth code->重定向回客户端(带code)->服务器带code和client secret请求第三方access token->拿到token之后就可以请求第三方资源了
3. 一个是授权协议，一个是令牌格式

---

### 4.5 SQL 注入

> 1. SQL 注入是什么？怎么产生的？
> 2. 如何防范？（参数化查询、ORM、最小权限）
> 3. 举一个拼接 SQL 导致注入的例子。

**你的回答：**
1. SQL注入是外部非法注入SQL到服务中，达到攻击系统或数据库的目的。通过前端表单提交的变量(非法sql)直接被拼接到sql中
2. 不要直接拼接sql，要参数化查询或者用ORM方式
3. `SELECT * FROM admin_user WHEN name=${uName}` uName来自客户端表单，这样直接拼接到sql的条件里会引起sql注入攻击，攻击者随便输入谁的用户名都能查到它的所有信息

---

### 4.6 接口安全

> 1. 如何防止接口被恶意刷？（限流：令牌桶 / 漏桶、IP 维度）
> 2. 如何设计幂等性接口？（防止重复提交：幂等键、唯一约束）
> 3. 除了 HTTPS，接口还需要哪些防护？（越权校验、敏感数据脱敏）

**你的回答：**
1. IP维度限流
2. 不知道
3. 接口资源需要跟权限系统对接，此外敏感用户数据需要脱敏处理

---

## 五、部署与运维（5 题）

### 5.1 部署方案

> 1. Node.js 应用如何部署？（PM2 / Docker / 云平台）
> 2. PM2 的常用功能？（cluster 模式、自动重启、日志管理）
> 3. 前端部署和后端部署有什么不同？

**你的回答：**
1. 本地服务通过Docker容器部署，用PM2管理应用进程；也可以用云平台的方式，那样更简单，云服务商提供简单的部署方式

---

### 5.2 Docker

> 1. Docker 的基本概念？（镜像、容器、Dockerfile、数据卷）
> 2. 编写 Dockerfile 的注意点？（多阶段构建、层缓存、基础镜像体积）
> 3. docker compose 用来干什么？

**你的回答：**
1. Docker有镜像、容器、数据卷、仓库、Dockerfile、docker-compose等基本概念：
镜像类比类的概念，容器是实例，数据卷是数据存储的地方，仓库是存储镜像的地方，Dockerfile是容器配方，docker-compose是多容器编排
2. 多阶段构建，层缓存，基础镜像体积
3. 用来编排多容器的，一个完整的应用通常有多个容器，app、数据库(mysql/redis等)；一个compose可以让它们一并运行

---

### 5.3 环境配置管理

> 1. 环境变量如何管理？（.env、dotenv、CI 注入）
> 2. 开发 / 测试 / 生产环境的配置差异如何管理？
> 3. 敏感信息如何保护？（密钥不进 Git、密钥管理服务）

**你的回答：**
1. .env+dotenv/shell环境变量或者CI注入
2. 用单独的配置文件管理，公共配置放common配置文件
3. 密钥不进代码，不进git

---

### 5.4 日志

> 1. 日志如何分级？（debug / info / warn / error）
> 2. 日志如何收集？（文件切割、ELK、云日志服务）
> 3. 日志里不该出现什么？（密码、token、敏感信息脱敏）

**你的回答：**

---

### 5.5 监控告警

> 1. 服务健康检查怎么做？（/health 接口、进程存活探测）
> 2. 常用监控指标？（CPU、内存、QPS、错误率、接口耗时 P99）
> 3. 如何做到告警"及时且不打扰"？（分级告警、基线对比）

**你的回答：**

---

## 六、性能与高并发（6 题）

### 6.1 Node 性能瓶颈

> 1. Node.js 适合什么业务？不适合什么？（I/O 密集 vs CPU 密集）
> 2. CPU 密集任务怎么处理？（worker_threads、子进程、拆成独立服务）
> 3. 如何提升 Node 服务的吞吐量？

**你的回答：**
1. 适合IO密集型，不太适合CPU密集型业务
2. workder_threads开线程进行cpu密集任务处理
3. 充分利用CPU多核能力，使用多进程集群cluster模式

---

### 6.2 内存排查

> 1. Node.js 内存泄漏的常见原因？（全局缓存无限增长、闭包、事件监听器不清理）
> 2. 如何定位内存泄漏？（--inspect、堆快照对比、clinic.js）
> 3. `--max-old-space-size` 是干什么的？默认堆上限是多少？

**你的回答：**
1. 不需要的全局缓存、闭包、事件监听器等没有及时清理
2. 不清楚
3. 设置堆内存容量，默认是2.4G

---

### 6.3 接口性能优化

> 1. 一个接口很慢，如何排查？（日志链路、数据库慢查询、外部依赖）
> 2. 优化手段有哪些？（缓存、索引、批量查询、并发 Promise.all）
> 3. 什么是 N+1 查询？如何避免？（ORM 预加载、联表）

**你的回答：**
1. 按照整条网络链路结合日志分析慢在哪个阶段？是数据查询慢还是网络IO还是数据计算慢
2. 数据查询慢可以用缓存、表索引、批量sql等方式；网络IO可以采取并发代替串行，同步改为异步；数据计算昂贵可以采用多线程避免主线程被阻塞
3. 就是N个数据，每个数据需要一次sql查询；批量sql以及联表查询都可以有效减少sql的查询次数

---

### 6.4 并发控制

> 1. 如何限制并发请求数？（信号量、队列）
> 2. 数据库连接池的作用？连接数过高会怎样？
> 3. 高并发下如何保护下游服务？（熔断、降级、限流）

**你的回答：**
1. 不知道
2. 数据库连接池是应用启动的时候提前与数据库建立连接；连接数过高可能会导致资源浪费，用不到的连接会被一直挂起
3. 熔断，降级处理以及限流都可以有效保护下游服务

---

### 6.5 消息队列

> 1. 消息队列解决什么问题？（削峰、解耦、异步化）
> 2. 常见 MQ 对比？（RabbitMQ、Kafka、Redis 的用途）
> 3. 什么场景必须用 MQ？（秒杀、订单异步通知、日志收集）

**你的回答：**
1. 高并发下削峰填谷；逻辑解耦，就像发布订阅事件一样，服务之间不需要知道对方是干啥的，只需要管自己的逻辑就好；异步化逻辑，不阻塞请求
2. 不知道
3. 秒杀下单，日志收集

---

### 6.6 分布式基础

> 1. 分布式锁怎么实现？（Redis SETNX + 过期时间）
> 2. 分布式锁有哪些坑？（锁过期导致并发、误删别人的锁——如何解决：value 校验 + Lua 脚本）
> 3. 微服务的基本组件有哪些？（服务注册发现、网关、配置中心、链路追踪）

**你的回答：**
1. redis可实现，利用redis的单线程特性，设置一个key作为自己的锁(存在key的话其他请求无法设置，进不来)+过期时间，一般手写的话需要自己实现锁凭证、过期续期以及清理的问题，因此生产上建议用redisson
2. 锁过期导致并发、误删别人的锁(因此锁需要凭证设计，自己管自己的锁)
3. 不知道

---

## 七、综合设计（4 题）

### 7.1 设计登录系统

> 设计一个用户登录系统：
> 1. 技术选型？（JWT 还是 Session？为什么）
> 2. 密码如何存储？登录失败如何处理？
> 3. 如何支持多端登录 / 单点登录？
> 4. 安全考虑？（防暴力破解、异地登录提醒）

**你的回答：**
1. 单点服务的内部后管系统用session，理由是实现简单，对安全性要求没那么高；多端登录或者涉及C端的用JWT，主要因为无状态可跨平台，且天然对抗CSRF攻击
2. 密码哈希加密存数据库，不能存明文；登录失败可设计忘记/重置密码等功能，实际上是通过别的方式认证(如邮件/手机验证码)之后重新设置密码更新用户数据库密码
3. 一次登录多端共用最关键在于jwt的secret需要共用，账号以及权限系统需要统一，这样从账号生产到验证都是统一的规则，不论哪个服务只要用上这套规则就能统一鉴权拿权限
4. 登录接口设置防暴力破解限制(比如超多少次锁1小时，连续操作累积封锁时间或者直接冻结)，通过IP识别异地登录并设计异地登录提醒

---

### 7.2 设计短链接服务

> 1. 短链接生成的算法？（发号器、hash + 冲突处理）
> 2. 跳转用 301 还是 302？为什么？
> 3. 如何统计点击量？（异步计数、Redis 计数器）
> 4. 数据量大了如何扩容？

**你的回答：**

---

### 7.3 设计文件上传服务

> 1. 上传方案？（单文件直传、分片上传、断点续传）
> 2. 大文件分片的思路？（前端分片 + 后端合并、秒传）
> 3. 上传如何校验？（类型白名单、大小限制、内容扫描）
> 4. 如何防止重名覆盖？（UUID 命名、对象存储）

**你的回答：**

---

### 7.4 接入第三方 API

> 1. 如何优雅地调用第三方 API？（超时、重试、降级）
> 2. 第三方接口的凭证如何管理？
> 3. 如何防止第三方故障拖垮自己？（熔断、超时、缓存兜底）
> 4. 你的 RAG 项目里怎么调 LLM API？（可结合 OpenAI / 通义 SDK、流式响应）

**你的回答：**

---

## 八、Node.js 手写题（6 题）

### 8.1 手写 Koa compose（洋葱模型）

> 手写 `compose` 实现洋葱模型中间件：
>
> ```js
> function compose(middlewares) {
>   // 返回 (ctx) => Promise
> }
>
> // 要求：按顺序执行，前一个中间件调用 next() 后，
> // 会先执行后一个中间件，再回到前一个中间件的后续代码
> ```

**你的回答：**
```js
function compose(middlewares) {
  return function (ctx) {
    let curIndex = -1 // 当前层级
    function dispatch(i) {
      if(i <= curIndex) {
        return Promise.reject('next recalled')
      }
      curIndex = i
      const fn = middlewares[i]
      if(!fn) return Promise.resolve()
      try{
        return Promise.resolve(fn(ctx, ()=>dispatch(i+1)))
      }catch(err){
        return Promise.reject(err)
      }
    }
    return dispatch(0)
  }
}
```

---

### 8.2 手写 Promise 并发控制

> 实现一个带并发限制的异步调度器（同时最多执行 n 个任务）：
>
> ```js
> async function concurrencyLimit(tasks, limit) {
>   // tasks: Promise 工厂函数数组
>   // 保证同时最多 limit 个任务在执行
> }
> ```

**你的回答：**
```js
// worker池实现方式1(用Promise.all)
async function concurrencyLimit(tasks, limit) {
  if(limit <= 0){
    throw new Error('limit必须大于0')
  }
  const total = tasks.length
  if(total === 0) return []
  const lanes = Math.min(total, limit)
  let index = 0
  const results = Array(total)
  function runOne(taskFactory) {
    return Promise.resolve().then(()=>taskFactory()).then((data)=>({status: 'fulfilled', data}), (err)=>({status: 'rejected', reason: err}))
  }
  async function worker() {
    while(index < total) {
      let i = index++
      results[i] = await runOne(tasks[i])
    }
  }
  await Promise.all(Array.from(lanes, () => worker()))
  return results
}

```
```js
// worker池实现方式2(不用Promise.all)
async function concurrencyLimit(tasks, limit) {
  if(limit <= 0){
    throw new Error('limit必须大于0')
  }
  const total = tasks.length
  if(total === 0) return []
  const workerCount = Math.min(total, limit)
  let index = 0
  const results = Array(total)
  let doneWorkers = 0
  function runOne(taskFactory) {
    return Promise.resolve().then(()=>taskFactory()).then((data)=>({status: 'fulfilled', data}), (err)=>({status: 'rejected', reason: err}))
  }
  async function worker() {
    while(index < total) {
      let i = index++
      results[i] = await runOne(tasks[i])
    }
  }
  // 自己控制worker池，则需要计数完成池个数
  return new Promise((resolve, reject)=>{
    for(let w=0; w<workerCount; w++){
      worker().then(()=>{
        if(++doneWorkers === workerCount) resolve(results)
      }, reject)
    }
  })
}

```
```js
//不用worker池，自己递归计数
function concurrencyLimit(tasks, limit) {
  return new Promise((resolve, reject)=>{
    if(limit<=0){
      reject(new Error('limit必须大于0'))
    }
    const total = tasks.length
    if(total === 0) return []
    let curIndex = -1
    let lane = 0
    let done = 0
    const results = Array(total)
    function runTask() {
      while(curIndex<total && lane <= limit) {
        lane++
        const i = ++curIndex
        Promise.resolve().then(()=>tasks[i]()).then((data)=>{
          results[i] = {status: 'fulfilled', data}
        }, (err)=>{
          results[i] = {status: 'rejected', data}
        }).finally(()=>{
          lane-- // 释放lane
          done++
          if(done === total) return resolve(results)
          runTask()
        })
      }
    }
    runTask()
  })
}
```
```js
// 自行实现all
function myPromiseAll(promises) {
  return new Promise((resolve, reject) => {
    const total = promises.length
    if(total === 0) return resolve([])
    const results = Array(total)
    let done = 0
    for(let i = 0; i < total; i++) {
      // 这里需要立即执行，所以把task[i]作为Promise.resolve的参数
      Promise.resolve(promises[i]).then((data) => {
        results[i] = data
        if(++done === total) resolve(results)
      }, reject)
    }
  })
}
// 自行实现allSettled
function myPromiseAllSettled(promises) {
  return new Promise(r => {
    const total = promises.length
    if(total === 0) r([])
    const results = Array(total)
    let done = 0
    for(let i = 0; i < total; i++) {
      Promise.resolve(promises[i]).then((value) => {
        results[i] = { status: 'fulfilled', value }
      }, (err) => {
        results[i] = { status: 'rejected', reason: err }
      }).finally(() => {
        if(++done === total) r(results)
      })
    }
  })
}
// 自行实现Race
function myPromiseRace(promises) {
  return new Promise((resolve, reject) => {
    const total = promises.length
    for(let i = 0; i < total; i++) {
      Promise.resolve(promises[i]).then(resolve, reject)
    }
  })
}
// 自行实现any
function myPromiseAny(promises) {
  return new Promise((resolve, reject) => {
    const total = promises.length
    if(total === 0) {
      return reject(new AggregateError([], 'All promises were rejected'))
    }
    let errCnt = 0
    const errors = new Array(total)
    for(let i = 0; i < total; i++) {
      Promise.resolve(promises[i]).then((data) => {
        resolve(data)
      }, (err) => {
        errors[i] = err
        if(++errCnt === total) {
          return reject(new AggregateError(errors, 'All promises were rejected'))
        }
      })
    }
  })
}
// 可重试的Promise高阶函数
const sleep = (delay) => new Promise(r => setTimeout(r, delay))
async function withRetry(fn, {
  times = 3, 
  delay = 100, 
  jitter = true, // 抖动开关
  shouldRetry = () => true 
}){
  for(let attempt = 1; ; attempt++) {
    try{
      return await fn(attempt) // attempt透明
    }catch(err){
      if(attempt >= times || !shouldRetry(err)){
        throw err 
      }
      const base = delay * 2 ** (attempt - 1)
      const wait = jitter ? base * (0.5 + Math.random()) : base
      await sleep(wait)
    }
  }
}
```

---

### 8.3 手写令牌桶限流器

> 实现一个简单的令牌桶限流器：
>
> ```js
> class TokenBucket {
>   constructor(capacity, rate) {
>     // capacity: 桶容量（最大令牌数）
>     // rate: 每秒补充速率
>   }
>   take() { ... } // 返回 true 表示允许通过，false 表示被限流
> }
> ```

**你的回答：**
```js
class TokenBucket {
  constructor(capacity, rate) {
    this.capacity = capacity
    this.rate = rate
    this.tokens = capacity
    this.lastRefill = Date.now()
  }
  refill() {
    const now = Data.now()
    const elapsed = (now - this.lastRefill) / 1000
    if(elapsed > 0) {
      this.tokens = Math.min(this.capacity, this.tokens + elapsed * this.rate)
      this.lastRefill = now
    }
  }
  tryTake(n = 1) {
    this.refill()
    if(this.tokens > n) {
      this.tokens -= n
      return true
    }
    return false
  }
  async take(n = 1) {
    for(;;) {
      this.refill()
      if(this.tokens > n) {
        this.tokens -= n
        return true
      }
      const waitMs = Math.ceil((n - this.tokens) / this.rate * 1000) // 向上取整，多等也比少等好
      await new Promise(r => setTimeout(r, waitMs))
    }
  }
}
```

---

### 8.4 手写指数退避重试

> 实现一个带指数退避的重试函数：
>
> ```js
> async function retry(fn, { times = 3, delay = 100 } = {}) {
>   // fn 为异步函数，失败后重试
>   // 重试间隔依次为 delay, delay*2, delay*4（指数退避）
>   // 全部失败后抛出最后一次错误
> }
> ```

**你的回答：**
```js
async function retry(fn, { times = 3, delay = 100, jitter = true, shouldRetry = () => true } = {}) {
  let result
  for(let attempt = 1; ; attempt++) {
    try {
      result = await fn(attempt)
    }catch(err) {
      if(++attempt === times || !shouldRetry(err)) throw err
      const base = delay * 2 ** (attempt - 1)
      const delayMs = jitter ? base * (Math.random() + 0.5) : base
      await new Promise(r => setTimeout(r, delayMs))
    }
  }
}
```

---

### 8.5 手写带 TTL 的缓存函数

> 实现带过期时间的函数结果缓存：
>
> ```js
> function memoizeWithTTL(fn, ttl = 60000) {
>   // 相同参数命中缓存直接返回结果
>   // 超过 ttl 后缓存失效，重新调用 fn
> }
> ```

**参考答案（增强版）：**

```js
// ========== ① 更好的 key：稳定序列化（属性顺序无关 + 循环引用安全） ==========
function stableStringify(value, seen = new WeakSet()) {
  if (value === null) return 'null'
  const t = typeof value
  if (t === 'number' || t === 'boolean') return String(value)
  if (t === 'string') return JSON.stringify(value)
  if (t === 'undefined') return 'undefined'
  if (t === 'function') return '[fn]'            // 函数用占位，不丢位置
  if (t === 'symbol') return value.toString()
  if (t === 'bigint') return value.toString() + 'n'

  if (seen.has(value)) return '[circular]'       // 循环引用：标记而非抛错
  seen.add(value)

  if (Array.isArray(value)) {
    return '[' + value.map((v) => stableStringify(v, seen)).join(',') + ']'
  }
  if (value instanceof Date) return 'Date:' + value.getTime()
  if (value instanceof Map) {
    return 'Map:{' + [...value.entries()]
      .map(([k, v]) => stableStringify(k, seen) + ':' + stableStringify(v, seen))
      .sort().join(',') + '}'
  }
  if (value instanceof Set) {
    return 'Set:{' + [...value].map((v) => stableStringify(v, seen)).sort().join(',') + '}'
  }
  // 普通对象：key 排序 → 顺序无关
  return '{' + Object.keys(value).sort()
    .map((k) => JSON.stringify(k) + ':' + stableStringify(value[k], seen))
    .join(',') + '}'
}

function defaultKey(args) {
  // 快速路径：单参数原始值直接转字符串（省掉序列化开销）
  if (args.length === 1) {
    const v = args[0]
    if (v === null || (typeof v !== 'object' && typeof v !== 'function')) return String(v)
  }
  return stableStringify(args)
}

// ========== ② Promise 超时（带定时器清理） ==========
function withTimeout(promise, ms) {
  if (!ms) return promise
  let timer
  const timeout = new Promise((_, reject) => {
    timer = setTimeout(() => reject(new Error(`memoize 超时(${ms}ms)`)), ms)
  })
  return Promise.race([promise, timeout]).finally(() => clearTimeout(timer))  // 清理定时器
}

// ========== ③ 完整实现 ==========
function memoizeWithTTL(fn, options = {}) {
  const {
    ttl = 60000,
    maxSize = 100,
    timeout = 0,                 // Promise 超时（0 = 不限）
    keyFn = defaultKey,
  } = options

  const cache = new Map()

  function memoized(...args) {
    const key = keyFn(args)
    const now = Date.now()
    const hit = cache.get(key)

    if (hit && now < hit.expireAt) {            // 命中
      cache.delete(key); cache.set(key, hit)    // LRU 提权
      return hit.value
    }

    let value = fn.apply(this, args)

    if (value instanceof Promise) {
      if (timeout > 0) value = withTimeout(value, timeout)   // 加超时
      value.catch(() => cache.delete(key))                   // 失败/超时都清缓存
    }

    cache.set(key, { value, expireAt: now + ttl })
    if (cache.size > maxSize) cache.delete(cache.keys().next().value)

    return value
  }

  // ===== 手动失效 =====
  memoized.delete = (...args) => cache.delete(keyFn(args))
  memoized.clear = () => cache.clear()
  Object.defineProperty(memoized, 'size', { get: () => cache.size })

  return memoized
}
```

**① key 的设计：四种方案对比**

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| `JSON.stringify(args)` | 直接序列化 | 一行搞定 | **属性顺序敏感**（`{a:1,b:2}` ≠ `{b:2,a:1}`）；`undefined`/函数/Symbol 丢失；循环引用抛错 |
| **稳定序列化**（默认） | 递归序列化 + **key 排序** | 顺序无关、特殊值安全、循环引用不崩 | ~30 行代码；深对象有开销 |
| 嵌套 Map / WeakMap | 不序列化，逐层 Map | 无序列化开销；WeakMap 能自动 GC | 对象按**引用**区分（内容相同也不命中）；实现复杂 |
| **自定义 `keyFn`** ⭐ | 调用方提供 key | 业务语义最清晰、性能最好 | 需调用方理解业务 |

**推荐组合**：默认走稳定序列化（+ 单参数快速路径），同时**暴露 `keyFn` 让调用方用业务 key**：

```js
// 调用方知道业务语义 → 直接用 id 做 key,比序列化整个对象快得多也更精确
const getUser = memoizeWithTTL(fetchUser, {
  ttl: 60_000,
  keyFn: (args) => `user:${args[0].id}`,
})
```

**② Promise 超时**

```js
function withTimeout(promise, ms) {
  if (!ms) return promise
  let timer
  const timeout = new Promise((_, reject) => {
    timer = setTimeout(() => reject(new Error(`memoize 超时(${ms}ms)`)), ms)
  })
  return Promise.race([promise, timeout]).finally(() => clearTimeout(timer))
}
```

两个要点：
1. **超时的 Promise 也要清缓存**——复用了"失败清缓存"的逻辑（`.catch(() => cache.delete(key))` 同时兜住 reject 和 timeout）
2. **`finally` 里 `clearTimeout`**——否则每次调用都残留一个定时器（内存泄漏，Node 进程还可能不退出）

**③ 手动失效**

```js
memoized.delete = (...args) => cache.delete(keyFn(args))   // 按参数删（用同一个 keyFn,保证能对上）
memoized.clear  = () => cache.clear()                      // 全清
Object.defineProperty(memoized, 'size', { get: () => cache.size })   // 只读 size
```

> 注意 `delete` 也用 `keyFn` 生成 key——**必须和写入时用同一套 key 规则**，否则删不掉。


**关键设计点：**

| # | 设计 | 说明 |
|---|------|------|
| 1 | `Map` 存 `{ value, expireAt }` | 记**绝对过期时间戳**（不是剩余时长），判断简单 |
| 2 | **惰性过期** | 读时判断 `now < expireAt`，不需要定时器；过期项被访问时自然覆盖 |
| 3 | **返回 `function` 而非箭头函数** | 保留调用方 `this`（配合 `fn.apply(this, args)`） |
| 4 | **缓存 Promise 本身** ⭐ | 并发调同一参数会**共享同一个 Promise** → 只发一次请求（**防缓存击穿**） |
| 5 | **失败清除缓存** ⭐ | 否则一次网络抖动，错误结果会被缓存整个 ttl |
| 6 | **LRU 容量上限** | `Map` 保持插入顺序，`cache.keys().next().value` 就是最旧的 key |

**实测验证：**

```
① 同参数调 3 次              → 执行 1 次     ✅ 命中缓存
② 150ms 后再调(ttl=100)      → 执行 2 次     ✅ 过期后重算
③ 不同参数                   → 各自独立缓存
④ 并发 3 次同参数             → 执行 1 次     ✅ 共享同一个 Promise(防击穿)
⑤ 异步失败后重调              → 执行 2 次     ✅ 错误没被缓存
⑥ this 绑定                  → 15           ✅ apply(this)
⑦ 容量上限 2 塞 3 个          → 最早的被淘汰后重算

增强部分:
⑧ {a:1,b:2} vs {b:2,a:1}     → 执行 1 次     ✅ 稳定 key(顺序无关)
⑨ 循环引用参数                → 不抛错        ✅
⑩ Promise 超时(50ms)         → 抛错 + 清缓存,重调重新执行 ✅
⑪ size / delete / clear      → 2 → 1 → 0    ✅ 手动失效
⑫ 自定义 keyFn(同 id 不同对象)→ 命中同一缓存  ✅
```

**面试追问准备：**

| 追问 | 回答要点 |
|------|---------|
| `JSON.stringify(args)` 做 key 有什么坑？ | 属性顺序敏感（`{a:1,b:2}` ≠ `{b:2,a:1}`）；`undefined`/函数/Symbol 会丢失；循环引用会抛错 → 严格场景应支持传入自定义 `keyFn` |
| 过期的 key 不删会内存泄漏吗？ | 会（长尾 key 多时）。三种解法：① 惰性删除（本实现）② 定时清理 ③ 容量上限 + LRU（本实现同时用了 ①③） |
| 缓存 Promise 有什么风险？ | 若 Promise 一直 pending（请求挂住），后续调用会**一起挂住** → 要给 fn 加超时（`Promise.race` + 定时器） |
| 多实例 / 多进程怎么办？ | 进程内缓存不共享 → 多实例要用 **Redis**（`SET key value EX ttl`） |
| 怎么支持手动失效？ | 在返回的函数上挂 `clear()` / `delete(...args)` 方法 |

> **一句话总结**：核心是 `Map` + `expireAt` 时间戳 + 惰性判断；**加分项是"缓存 Promise"（防击穿）和"失败清除"**——这两点能区分"背过题"和"真用过"。

---

### 8.6 手写简易Promise

**你的回答：**
```js
class MyPromise {
  constructor(executor) {
    this.state = 'pending'
    this.callbacks = []
    this.value = null

    const resolve = (val) => this.#transition('fulfilled', val)
    const reject = (val) => this.#transition('rejected', val)
    
  }
  #transition(state, value) {
    if(this.state !== 'pending') return // 状态不可逆
    this.state = state
    this.value = value
    queueMicroTask(() => this.callbacks.forEach(cb => cb(this)))
  }
  then(onFulfilled, onRejected) {
    return new MyPromise((resolve, reject) => {
      // 最关键的设计
      const callback = (promise) => {
        try {
          const fn = promise.state === 'fulfilled' ? onFulfilled : onRejected
          const result = fn ? fn(promise.value) : promise.value
          result instanceof MyPromise ? result.then(resolve, reject) : resolve(result)
        } catch (err) {
          reject(err)
        }
      }
      this.state === 'pending' ? this.callbacks.push(callback) : callback(this)
    })
  }
  catch(onRejected) {
    return this.then(null, cb)
  }
  static resolve(val){
    return new MyPromise((resolve) => resolve(val))
  }
  static reject(val){
    return new MyPromise((_, reject) => reject(val))
  }
}
```
