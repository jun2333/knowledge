# MySQL 专项模拟面试题 - 2026.09.10

> 面向后端 / 全栈方向，从基础概念到索引优化、事务锁、实战设计，由易到难共 39 题。
> 每道题下方留有作答区，建议先独立作答再对照资料查漏补缺。
> 第二、六部分的 SQL 题基于下面的**示例库**，建议先在本地建表跑一遍。

---

## 示例库（SQL 题通用）

```sql
-- 部门表
CREATE TABLE department (
  id   INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50) NOT NULL COMMENT '部门名'
);

-- 员工表
CREATE TABLE employee (
  id         INT PRIMARY KEY AUTO_INCREMENT,
  name       VARCHAR(50)   NOT NULL COMMENT '姓名',
  dept_id    INT           COMMENT '部门ID',
  salary     DECIMAL(10,2) NOT NULL COMMENT '月薪',
  hire_date  DATE          NOT NULL COMMENT '入职日期',
  manager_id INT           COMMENT '直属上级ID'
);

-- 订单表
CREATE TABLE orders (
  id         BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id    INT           NOT NULL,
  amount     DECIMAL(10,2) NOT NULL COMMENT '金额',
  status     TINYINT       NOT NULL DEFAULT 0 COMMENT '0待支付 1已支付 2已取消',
  created_at DATETIME      NOT NULL,
  KEY idx_user_created (user_id, created_at)
);

-- 示例数据
INSERT INTO department (id, name) VALUES
  (1, '研发部'), (2, '市场部'), (3, '人事部');

INSERT INTO employee (id, name, dept_id, salary, hire_date, manager_id) VALUES
  (1, '张三', 1, 25000, '2020-03-01', NULL),
  (2, '李四', 1, 18000, '2021-06-15', 1),
  (3, '王五', 1, 18000, '2022-01-10', 1),
  (4, '赵六', 2, 15000, '2021-09-01', NULL),
  (5, '钱七', 2, 12000, '2023-02-20', 4),
  (6, '孙八', 3, 10000, '2022-07-01', NULL),
  (7, '周九', NULL, 8000, '2023-05-01', NULL);   -- 无部门

INSERT INTO orders (user_id, amount, status, created_at) VALUES
  (1, 100.00, 1, '2026-09-01 10:00:00'),
  (1, 200.00, 1, '2026-09-02 11:00:00'),
  (1, 150.00, 1, '2026-09-03 12:00:00'),
  (2, 300.00, 0, '2026-09-02 12:00:00'),
  (3, 150.00, 2, '2026-09-03 09:00:00');
```

---

## 一、基础概念（8 题）

### 1.1 数据库基本概念

> 解释：表、行、列、主键、外键、索引、视图，各自是什么、解决什么问题？

**你的回答：**


---

### 1.2 数据类型怎么选

> 1. `VARCHAR(50)` 和 `CHAR(50)` 的区别？分别适合什么场景？
> 2. 存金额为什么用 `DECIMAL` 而不是 `FLOAT` / `DOUBLE`？
> 3. `DATETIME` 和 `TIMESTAMP` 的区别？（范围、时区、占用空间）
> 4. `INT` 和 `BIGINT` 怎么选？主键为什么要考虑 BIGINT？

**你的回答：**


---

### 1.3 SQL 语句分类

> SQL 分为 DDL / DML / DQL / DCL / TCL 五类，各是什么？每类举 2 个例子。

**你的回答：**


---

### 1.4 NULL 有哪些坑

> 1. `WHERE col = NULL` 为什么查不到数据？正确写法是什么？
> 2. `COUNT(*)`、`COUNT(1)`、`COUNT(col)` 的区别？
> 3. `NULL` 参与聚合 / 运算会怎样？
> 4. 唯一索引里可以有多个 NULL 吗？
> 5. `ORDER BY col` 时 NULL 排在最前还是最后？

**你的回答：**


---

### 1.5 主键 vs 唯一索引 vs 普通索引

> 三者的区别？分别限制什么、一张表能建几个？

**你的回答：**


---

### 1.6 为什么 InnoDB 推荐自增主键

> 1. 用业务字段（如身份证号）做主键有什么问题？
> 2. 用 UUID 做主键有什么问题？

**你的回答：**


---

### 1.7 DELETE / TRUNCATE / DROP

> 三者区别（删除范围、能否回滚、是否重置自增、是否触发触发器、执行速度）。

**你的回答：**


---

### 1.8 UNION vs UNION ALL

> 区别是什么？性能上哪个更好？什么场景**必须**用 UNION？

**你的回答：**


---

## 二、SQL 查询基础（8 题）

### 2.1 建表与 CRUD

> 基于示例库写 SQL：
> 1. 给 employee 表增加 `email VARCHAR(100)` 字段
> 2. 新增一名员工（自己编数据）
> 3. 把研发部所有员工工资 +1000
> 4. 删除没有部门的员工

**你的回答：**

```sql
-- 在此作答

```

---

### 2.2 条件查询与排序

> 1. 查出工资在 12000~20000 之间的员工，按工资降序
> 2. 查出 2021 年之后入职的员工
> 3. 查出姓名以"张"开头的员工
> 4. 分页：查第 2 页，每页 3 条（按 id 升序）

**你的回答：**

```sql
-- 在此作答

```

---

### 2.3 分组聚合

> 1. 统计每个部门的员工人数、平均工资、最高工资
> 2. 只保留平均工资大于 15000 的部门
> 3. 统计全公司总人数和总工资

**你的回答：**

```sql
-- 在此作答

```

---

### 2.4 多表连接

> 1. 查出每个员工的名字和所属部门名（**包括没有部门的员工**）
> 2. 查出**没有员工**的部门

**你的回答：**

```sql
-- 在此作答

```

---

### 2.5 子查询

> 查出工资**高于本部门平均工资**的员工，显示：姓名、部门名、工资、本部门平均工资。

**你的回答：**

```sql
-- 在此作答

```

---

### 2.6 每组 TopN（经典题）

> 查出每个部门工资**最高的前 2 名**员工（部门内工资相同也算并列，先按你的理解实现）。

**你的回答：**

```sql
-- 在此作答

```

---

### 2.7 存在性判断

> 查出**从未下过订单**的用户。
> 追问：`NOT IN` 和 `NOT EXISTS` 有什么区别？NULL 会带来什么坑？

**你的回答：**

```sql
-- 在此作答

```

---

### 2.8 窗口函数

> 1. 给每个部门的员工按工资从高到低排名（**同薪同名次**）
> 2. 计算每个员工的工资占**本部门总工资**的百分比

**你的回答：**

```sql
-- 在此作答

```

---

## 三、索引与执行计划（7 题）

### 3.1 索引的数据结构

> 1. 索引为什么能加快查询？
> 2. MySQL 为什么选 B+ 树，而不是 B 树 / 哈希表 / 红黑树？

**你的回答：**


---

### 3.2 聚簇索引与回表

> 1. 什么是聚簇索引？InnoDB 的主键索引和二级索引结构有什么不同？
> 2. 什么是"回表"？什么是"覆盖索引"？如何避免回表？

**你的回答：**


---

### 3.3 联合索引与最左前缀

> 有联合索引 `(a, b, c)`，判断下面哪些查询能用上索引、用到哪几列：
> 1. `WHERE a = 1`
> 2. `WHERE a = 1 AND b = 2`
> 3. `WHERE b = 2 AND c = 3`
> 4. `WHERE a = 1 AND c = 3`
> 5. `WHERE a = 1 AND b > 2 AND c = 3`
> 6. `WHERE a = 1 ORDER BY b`
>
> 追问：为什么范围查询后面的列用不上索引？

**你的回答：**


---

### 3.4 索引失效的场景

> 列举至少 5 种索引失效（或无法有效使用索引）的情况，并说明原因。
> 提示：函数运算、隐式类型转换、`LIKE '%x'`、`OR`、最左前缀……

**你的回答：**


---

### 3.5 读懂 EXPLAIN

> `EXPLAIN` 结果里这几列分别看什么：`type`、`key`、`rows`、`Extra`？
> 1. `type` 有哪些常见值，从好到坏怎么排？
> 2. `Extra` 里出现什么说明有问题（如 `Using filesort`、`Using temporary`）？

**你的回答：**


---

### 3.6 深分页优化

> 1. `SELECT * FROM t LIMIT 1000000, 20` 为什么慢？
> 2. 有哪些优化方案？（至少两种，说明原理）

**你的回答：**


---

### 3.7 索引下推（ICP）

> 什么是索引下推（Index Condition Pushdown）？它减少了什么开销？
> 举例：联合索引 `(name, age)`，查询 `WHERE name LIKE '张%' AND age = 20`，没有 ICP 和有 ICP 分别怎么执行？

**你的回答：**


---

## 四、事务与锁（6 题）

### 4.1 ACID

> 解释 ACID 四个特性，并说明 InnoDB 分别靠什么实现（redo log / undo log / MVCC / 锁）。

**你的回答：**


---

### 4.2 隔离级别

> 1. 四种隔离级别是什么？MySQL 默认是哪个？
> 2. 脏读、不可重复读、幻读分别是什么？各在哪个级别被解决？

**你的回答：**


---

### 4.3 MVCC

> 1. MVCC 是什么？解决什么问题？
> 2. 它的实现依赖哪三样东西（隐藏字段 / undo log 版本链 / ReadView）？
> 3. "快照读"和"当前读"的区别？哪些语句属于当前读？

**你的回答：**


---

### 4.4 InnoDB 的锁

> 1. 行锁、表锁、间隙锁、临键锁（Next-Key Lock）、意向锁分别是什么？
> 2. 什么情况下行锁会"升级"成表锁？
> 3. 间隙锁解决什么问题？它有什么副作用？

**你的回答：**


---

### 4.5 死锁

> 1. 死锁是怎么产生的？举一个两个事务互相等待的例子。
> 2. 怎么排查死锁？（`SHOW ENGINE INNODB STATUS` 看什么）
> 3. 怎么减少死锁？（至少三条实践建议）

**你的回答：**


---

### 4.6 乐观锁 vs 悲观锁

> 1. 两者的思路区别？分别怎么实现（版本号 / `SELECT ... FOR UPDATE`）？
> 2. 扣库存场景用哪个？写出乐观锁的 SQL。
> 3. 乐观锁失败重试要注意什么？

**你的回答：**

```sql
-- 在此作答

```

---

## 五、实战与设计（5 题）

### 5.1 一条 SQL 的执行流程

> 从客户端发出 SQL 到返回结果，中间经过哪些环节？
> （连接器 → 解析器 → 优化器 → 执行器 → 存储引擎……各做什么）

**你的回答：**


---

### 5.2 慢查询排查

> 线上发现一个接口很慢，怀疑是 SQL 问题。写出你的完整排查思路（从定位问题到优化验证）。

**你的回答：**


---

### 5.3 高并发扣库存

> 有 100 件库存、1000 个并发请求，怎么保证不超卖？对比几种方案：
> 1. `SELECT ... FOR UPDATE`（悲观锁）
> 2. 乐观锁（`UPDATE ... WHERE stock > 0` 条件更新）
> 3. Redis 预扣减 + 异步落库
>
> 各有什么优缺点？你选哪个，为什么？

**你的回答：**


---

### 5.4 缓存与数据库一致性

> 1. 为什么常见做法是"先更新数据库、再删除缓存"，而不是"先删缓存、再更新数据库"？
> 2. 什么是延迟双删？它解决什么问题？
> 3. 缓存和数据库能做到强一致吗？为什么？

**你的回答：**


---

### 5.5 分库分表

> 1. 什么信号说明该分库分表了？（数据量、QPS、单表大小）
> 2. 水平分表和垂直分表的区别？
> 3. 分表后会带来哪些新问题？（跨库 JOIN、分布式事务、全局 ID、分页）

**你的回答：**


---

## 六、写 SQL 综合题（5 题）

### 6.1 连续登录

> 基于 `orders` 表（把 `created_at` 的日期当作"活跃日"），查出**连续 3 天及以上**有订单的用户。
> 提示：`日期 - ROW_NUMBER()` 分组法。

**你的回答：**

```sql
-- 在此作答

```

---

### 6.2 行转列

> 有成绩表 `score(student_id, subject, score)`，把它转成"每行一个学生、各科目作为列"的格式。

**你的回答：**

```sql
-- 在此作答

```

---

### 6.3 分组取每组前 N（窗口函数版）

> 用窗口函数重写 2.6：每个部门工资最高的前 2 名。

**你的回答：**

```sql
-- 在此作答

```

---

### 6.4 环比 / 同比

> 基于 orders 表，统计**每个已支付订单**的月度金额，并计算**环比**（与上月相比的增长率）。
> 提示：`LAG()` 窗口函数。

**你的回答：**

```sql
-- 在此作答

```

---

### 6.5 留存 / 漏斗

> 基于 orders 表，计算**次日留存率**：某天有订单的用户中，第二天仍然有订单的比例。

**你的回答：**

```sql
-- 在此作答

```

---

## 附：自检清单

- [ ] 第一部分基础概念能否不看资料答出？
- [ ] 第二部分 SQL 题能否独立写出（不看答案）？
- [ ] 索引三大块（原理 / 最左前缀 / 失效场景）是否真的理解，而不是背结论？
- [ ] 事务与 MVCC 能否讲清"为什么能解决不可重复读和幻读"？
- [ ] 实战题能否结合自己的项目场景（mall / RAG 服务）讲？
