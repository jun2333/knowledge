# 模拟面试基础题 - 2026.08.07

> 以一线大厂高频经典题为主，覆盖 JavaScript、浏览器、HTML/CSS、Vue、React、网络与工程化、手写代码、综合项目八个方向，偏向基础但覆盖全面。
> 不含 TypeScript 题目（已有专门一套，见 2026-08-05）。
> 每道题下方留有答题区域，建议先独立作答再对照资料查漏补缺。

---

## 一、JavaScript 基础（8 题）

### 1.1 var / let / const 的区别

> 1. `var`、`let`、`const` 有什么区别？
> 2. 什么是变量提升（hoisting）？什么是暂时性死区（TDZ）？
> 3. 以下代码分别输出什么？为什么？
>
> ```js
> console.log(a); // ?
> var a = 1;
>
> console.log(b); // ?
> let b = 2;
>
> const c = { name: 'a' };
> c.name = 'b'; // 会报错吗？
> ```

**你的回答：**

---

### 1.2 闭包

> 1. 什么是闭包？闭包的形成条件是什么？
> 2. 闭包有哪些实际应用场景？（至少 2 个）
> 3. 闭包会造成内存泄漏吗？如何避免？
> 4. 以下代码输出什么？把 `var` 改成 `let` 呢？为什么？
>
> ```js
> for (var i = 0; i < 5; i++) {
>   setTimeout(() => console.log(i), 100);
> }
> ```

**你的回答：**

---

### 1.3 this 指向

> 1. `this` 的指向有哪几种情况？分别举例（全局、普通函数、对象方法、箭头函数、构造函数、事件回调）。
> 2. 以下代码输出什么？
>
> ```js
> const obj = {
>   name: 'obj',
>   fn: function () {
>     console.log(this.name);
>   },
>   arrow: () => {
>     console.log(this.name);
>   }
> };
> obj.fn();       // ?
> obj.arrow();    // ?
> const fn = obj.fn;
> fn();           // ?
> ```
>
> 3. `call` / `apply` / `bind` 的区别？

**你的回答：**

---

### 1.4 事件循环

> 1. 宏任务（macrotask）和微任务（microtask）分别有哪些？
> 2. 浏览器事件循环的执行机制是什么？
> 3. 以下代码的输出顺序？
>
> ```js
> console.log('1');
> setTimeout(() => console.log('2'), 0);
> Promise.resolve().then(() => console.log('3'));
> console.log('4');
> ```
>
> 4. `async / await` 在事件循环中如何工作？`await` 后面的代码什么时候执行？

**你的回答：**

---

### 1.5 Promise 基础

> 1. Promise 有哪几种状态？状态可以逆转吗？
> 2. `Promise.all` / `Promise.race` / `Promise.allSettled` 的区别？各自的适用场景？
> 3. 以下代码输出什么？
>
> ```js
> Promise.resolve(1)
>   .then(v => { console.log(v); return v + 1; })
>   .then(v => { console.log(v); throw new Error('err'); })
>   .catch(e => console.log('catch'))
>   .then(v => console.log('finally', v));
> ```

**你的回答：**

---

### 1.6 深浅拷贝

> 1. 浅拷贝和深拷贝的区别？
> 2. `JSON.parse(JSON.stringify(obj))` 有哪些局限性？（函数、undefined、Symbol、循环引用、Date、RegExp 等）
> 3. 实现浅拷贝有哪些方法？（Object.assign、展开运算符、slice/concat 等）

**你的回答：**

---

### 1.7 new 原理

> 1. `new` 操作符执行时做了哪几件事？
> 2. 手写实现一个 `new`。
> 3. 构造函数返回一个对象时，`new` 的结果是什么？

**你的回答：**

---

### 1.8 类型转换

> 1. `==` 和 `===` 的区别？
> 2. 以下表达式的结果是什么？为什么？
>
> ```js
> [] == ![]           // ?
> 0 == ''             // ?
> null == undefined   // ?
> NaN === NaN         // ?
> [1, 2] == '1,2'     // ?
> ```
>
> 3. `typeof` 和 `instanceof` 的区别？`typeof null` 的结果是什么？

**你的回答：**

---

## 二、浏览器（8 题）

### 2.1 从输入 URL 到页面渲染

> 从输入 URL 到页面渲染完成，整个过程发生了什么？请分阶段描述（DNS 解析 → TCP 连接 → HTTP 请求 → 浏览器渲染 → 资源加载）。

**你的回答：**

---

### 2.2 浏览器缓存

> 1. 强缓存和协商缓存的区别？
> 2. 分别对应哪些响应头？（Cache-Control / Expires / ETag / Last-Modified）
> 3. 为什么需要缓存？强缓存命中时浏览器还会发请求吗？
> 4. 正常刷新、强制刷新（Ctrl+F5）时缓存策略有何不同？

**你的回答：**

---

### 2.3 渲染流程与回流重绘

> 1. 浏览器渲染页面的完整流程？（HTML → DOM、CSS → CSSOM、渲染树、布局、绘制、合成）
> 2. 什么是回流（reflow）和重绘（repaint）？哪些操作会触发回流？
> 3. 如何减少回流和重绘？（class 合并、display: none 批量操作、文档片段、will-change 等）

**你的回答：**

---

### 2.4 事件流与事件委托

> 1. 事件冒泡和事件捕获的执行顺序？
> 2. `addEventListener` 的第三个参数有什么用？
> 3. 如何阻止事件冒泡？如何阻止默认行为？两者有什么区别？
> 4. 什么是事件委托？有什么好处？举例说明。

**你的回答：**

---

### 2.5 本地存储

> 1. cookie、localStorage、sessionStorage 的区别？（大小、生命周期、作用域、是否随请求发送）
> 2. cookie 有哪些常用属性？（httpOnly、secure、SameSite、maxAge 等）
> 3. 什么场景会用到 IndexedDB？

**你的回答：**

---

### 2.6 跨域

> 1. 什么是同源策略？为什么浏览器要限制跨域？
> 2. 跨域的解决方案有哪些？（JSONP、CORS、代理、postMessage 等）
> 3. CORS 的简单请求和预检请求（preflight）是什么？什么情况会触发预检？

**你的回答：**

---

### 2.7 前端安全

> 1. XSS 攻击是什么？有哪些类型？如何防范？
> 2. CSRF 攻击是什么？如何防范？
> 3. 为什么说 HTTPS 能防中间人攻击？

**你的回答：**

---

### 2.8 垃圾回收与内存泄漏

> 1. 浏览器的垃圾回收机制是怎样的？（标记清除、V8 分代回收）
> 2. 常见的内存泄漏场景有哪些？（全局变量、闭包、定时器、事件监听、脱离 DOM 的引用）
> 3. 如何排查内存泄漏？（Chrome DevTools 的 Performance / Memory 面板、堆快照对比）

**你的回答：**

---

## 三、HTML / CSS（8 题）

### 3.1 盒模型

> 1. 什么是盒模型？由哪几部分组成？
> 2. 标准盒模型和怪异盒模型（IE 盒模型）的区别？
> 3. `box-sizing: border-box` 有什么作用？为什么推荐全局使用？

**你的回答：**

---

### 3.2 水平垂直居中

> 1. 一个元素水平垂直居中，有哪些实现方式？（至少 4 种）
> 2. 如果子元素尺寸不固定，哪些方式仍然有效？

**你的回答：**

---

### 3.3 Flex 布局

> 1. Flex 容器的常用属性有哪些？分别控制什么？
> 2. `flex: 1` 表示什么？等价于哪几个属性的组合？
> 3. 如何用 Flex 实现一个左固定右自适应的两栏布局？

**你的回答：**

---

### 3.4 BFC

> 1. 什么是 BFC？
> 2. 如何触发 BFC？（overflow: hidden、float、display: inline-block/flex、position: absolute 等）
> 3. BFC 解决了哪些经典问题？（margin 塌陷、浮动元素高度塌陷、垂直 margin 合并）

**你的回答：**

---

### 3.5 选择器优先级

> 1. CSS 选择器的优先级如何计算？
> 2. 各种选择器的权重是多少？（!important、内联、id、class、元素）
> 3. 使用 `!important` 有什么注意点？

**你的回答：**

---

### 3.6 定位

> 1. `position` 有哪些取值？分别有什么特点？
> 2. `fixed` 和 `absolute` 的区别？
> 3. `sticky` 定位是什么？典型使用场景？

**你的回答：**

---

### 3.7 伪元素与伪类

> 1. 伪元素和伪类的区别？
> 2. `::before` / `::after` 有哪些经典用法？（清除浮动、图标、气泡角标等）
> 3. `:nth-child(n)` 和 `:nth-of-type(n)` 的区别？

**你的回答：**

---

### 3.8 移动端适配

> 1. 移动端适配有哪些方案？（rem、vw/vh、媒体查询、流式布局）
> 2. rem 的原理是什么？rem 和 em 的区别？
> 3. viewport meta 标签的作用？（width=device-width, initial-scale=1）
> 4. 1px 边框问题怎么解决？

**你的回答：**

---

## 四、Vue（8 题）

### 4.1 v-if vs v-show

> 1. `v-if` 和 `v-show` 的区别？
> 2. 什么场景用 `v-if`，什么场景用 `v-show`？
> 3. `v-if` 和 `v-for` 一起用会有什么问题？

**你的回答：**

---

### 4.2 computed vs watch

> 1. `computed` 和 `watch` 的区别？
> 2. `computed` 为什么有缓存？
> 3. 什么场景用 `computed`，什么场景用 `watch`？

**你的回答：**

---

### 4.3 响应式原理

> 1. Vue 2 的响应式原理？（Object.defineProperty + 依赖收集 + 派发更新）
> 2. Vue 2 响应式的局限性？（新增属性不响应、数组索引、性能）
> 3. Vue 3 为什么用 Proxy？相比 defineProperty 有哪些优势？

**你的回答：**

---

### 4.4 组件通信

> 1. Vue 组件间通信有哪些方式？
> 2. 各自的适用场景？（父子：props/emit；跨层级：provide/inject；全局：Pinia；兄弟：事件总线）
> 3. 如何理解 props 的单向数据流？

**你的回答：**

---

### 4.5 生命周期

> 1. Vue 3 的生命周期有哪些钩子？（setup、onMounted、onUpdated、onUnmounted 等）
> 2. 父子组件挂载和更新的执行顺序？
> 3. 什么场景会用到 onMounted / onUnmounted？（DOM 操作、事件监听清理）

**你的回答：**

---

### 4.6 key 的作用

> 1. `v-for` 为什么要加 key？
> 2. 用 index 作为 key 有什么问题？什么场景会出 bug？
> 3. key 在 diff 中是如何起作用的？（同层比较、节点复用）

**你的回答：**

---

### 4.7 v-model

> 1. `v-model` 在表单元素上是怎么实现的？
> 2. 自定义组件上怎么使用 `v-model`？（modelValue / update:modelValue）
> 3. `.sync` 修饰符和 `v-model` 的关系？

**你的回答：**

---

### 4.8 nextTick

> 1. `nextTick` 是做什么的？什么场景需要用到？
> 2. 为什么数据变了 DOM 不会立即更新？（异步更新队列）
> 3. `nextTick` 的实现原理？（微任务优先，降级宏任务）

**你的回答：**

---

## 五、React（9 题）

### 5.1 组件通信

> 1. React 组件间如何通信？（props 逐层传递、状态提升、Context、全局状态管理）
> 2. Context 的使用场景和注意事项？（Provider value 变化会导致所有消费者重新渲染）

**你的回答：**

---

### 5.2 类组件生命周期

> 1. 类组件生命周期分为哪几个阶段？各阶段有哪些钩子？
> 2. componentDidMount、componentDidUpdate、componentWillUnmount 分别做什么？
> 3. React 16.3 之后废弃了哪些生命周期？为什么？（componentWillReceiveProps 等在 Fiber 下的问题）

**你的回答：**

---

### 5.3 useEffect

> 1. `useEffect` 的作用？依赖数组为 `[]`、`[dep]`、不传三种情况的执行时机？
> 2. `useEffect` 和 `useLayoutEffect` 的区别？什么场景用 useLayoutEffect？
> 3. `useEffect` 的清理函数是做什么的？什么时候执行？

**你的回答：**

---

### 5.4 常用 Hooks

> 1. useState / useMemo / useCallback / useRef 各自的作用？
> 2. `useMemo` 和 `useCallback` 的区别？各自的使用场景？
> 3. `useRef` 能用来做什么？（DOM 引用、保存可变值）为什么不能用普通变量代替？

**你的回答：**

---

### 5.5 Hooks 规则与闭包陷阱

> 1. 为什么 Hooks 不能写在条件语句、循环里？（Hooks 链表、调用顺序依赖）
> 2. 什么是闭包陷阱？useEffect 依赖数组漏传会有什么问题？
> 3. 如何解决闭包陷阱？（依赖数组、useRef、useCallback）

**你的回答：**

---

### 5.6 受控 vs 非受控

> 1. 受控组件和非受控组件的区别？
> 2. 各自优缺点？什么时候用非受控组件？
> 3. 如何把非受控组件转换成受控组件？

**你的回答：**

---

### 5.7 虚拟 DOM 与 diff

> 1. 虚拟 DOM 是什么？为什么需要它？（声明式、跨平台、性能）
> 2. React diff 算法的基本策略？（同层比较、类型不同直接替换、key 复用）
> 3. key 的作用？用 index 当 key 有什么问题？

**你的回答：**

---

### 5.8 setState

> 1. `setState` 是同步还是异步？什么时候是同步的？
> 2. 为什么 React 要合并批量更新？
> 3. `setState` 后能立刻拿到最新值吗？拿不到怎么办？（回调、useEffect）

**你的回答：**

---

### 5.9 性能优化

> 1. React 性能优化手段有哪些？（React.memo、PureComponent、useMemo、useCallback、key、懒加载）
> 2. `React.memo` 和 `useMemo` 的区别？
> 3. 为什么父组件重新渲染时子组件也会重新渲染？如何避免？

**你的回答：**

---

## 六、网络与工程化（6 题）

### 6.1 HTTP vs HTTPS

> 1. HTTP 和 HTTPS 的区别？
> 2. HTTPS 的加密流程？（对称加密 + 非对称加密 + CA 证书）
> 3. HTTPS 一定安全吗？主要风险在哪？

**你的回答：**

---

### 6.2 HTTP 状态码

> 1. 常见 HTTP 状态码有哪些？（200、201、204、301、302、304、400、401、403、404、500、502、503）
> 2. 301 和 302 的区别？
> 3. 304 是怎么产生的？（协商缓存命中）

**你的回答：**

---

### 6.3 GET vs POST

> 1. GET 和 POST 的区别？（参数位置、长度限制、缓存、历史记录、语义）
> 2. GET 请求能带 body 吗？
> 3. 什么时候用 POST？（提交表单、敏感数据、大数据量）

**你的回答：**

---

### 6.4 TCP 三次握手与四次挥手

> 1. TCP 三次握手的过程？为什么是三次而不是两次？
> 2. 四次挥手的过程？为什么挥手要四次？
> 3. 为什么 TIME_WAIT 状态要等待 2MSL？

**你的回答：**

---

### 6.5 WebSocket

> 1. WebSocket 和 HTTP 的区别？
> 2. WebSocket 的应用场景？（聊天、协作、实时通知）
> 3. WebSocket 如何保持长连接？（心跳机制）

**你的回答：**

---

### 6.6 Webpack 核心概念

> 1. Webpack 的核心概念？（entry、output、loader、plugin、mode）
> 2. Loader 和 Plugin 的区别？各举几个常用的例子？
> 3. 简述 Webpack 的构建流程？（解析入口 → 构建依赖图 → 转换 → 打包输出）

**你的回答：**

---

## 七、手写代码（8 题）

### 7.1 数组去重

> 实现数组去重，至少写出 3 种方式，并说明各自优缺点。
>
> ```js
> const arr = [1, 1, '1', '1', NaN, NaN, { a: 1 }, { a: 1 }];
> // 注意：NaN === NaN 为 false，对象去重按引用
> ```

**你的回答：**

---

### 7.2 防抖与节流

> 1. 什么是防抖（debounce）？什么是节流（throttle）？各自的应用场景？
> 2. 手写实现 debounce 和 throttle（基础版即可）。

**你的回答：**

---

### 7.3 手写 call / apply / bind

> 手写实现：
> 1. `Function.prototype.call`
> 2. `Function.prototype.apply`
> 3. `Function.prototype.bind`（需要考虑被 `new` 调用的情况）

**你的回答：**

---

### 7.4 手写深拷贝

> 手写深拷贝，要求：
> 1. 支持数组和普通对象
> 2. 解决循环引用
> 3. 说明 JSON.parse(JSON.stringify()) 的局限性

**你的回答：**

---

### 7.5 手写 Promise.all

> 手写 `Promise.all`，要求：
> 1. 输入一个 Promise 数组，返回一个新的 Promise
> 2. 全部成功则返回结果数组（保持传入顺序）
> 3. 任一失败则立即 reject
> 4. 注意空数组的边界情况

**你的回答：**

---

### 7.6 手写发布订阅

> 手写一个简易的发布订阅（EventEmitter），要求实现：
> 1. `on(event, handler)`：订阅
> 2. `emit(event, ...args)`：发布
> 3. `off(event, handler)`：取消订阅
> 4. `once(event, handler)`：只执行一次

**你的回答：**

---

### 7.7 手写 instanceof

> 手写 `instanceof` 的实现，并解释其原理：
>
> ```js
> function myInstanceof(obj, Constructor) {
>   // ...
> }
> ```

**你的回答：**

---

### 7.8 手写柯里化

> 手写柯里化函数：
>
> ```js
> function curry(fn) {
>   // ...
> }
>
> const add = (a, b, c) => a + b + c;
> const curriedAdd = curry(add);
> curriedAdd(1)(2)(3); // 6
> ```

**你的回答：**

---

## 八、综合与项目（4 题）

### 8.1 性能优化框架

> 如果让你系统性地做一次前端性能优化，你会从哪些方面入手？请给出你的分析框架和优先级。
>
> （提示：加载性能、渲染性能、运行时性能，以及如何量化优化收益）

**你的回答：**

---

### 8.2 首屏排查

> 线上页面首屏加载很慢，你会如何排查？请按步骤说明（浏览器 DevTools 怎么用、看哪些指标、怎么定位瓶颈）。

**你的回答：**

---

### 8.3 最有挑战的项目

> 讲一个你做过的最有挑战的项目：
> 1. 项目背景和目标是什么？
> 2. 你在其中承担什么角色？
> 3. 遇到的最大难点是什么？怎么解决的？
> 4. 最终结果如何？有什么复盘收获？

**你的回答：**

---

### 8.4 软性问题

> 1. 你最近在学习什么新技术？为什么学它？
> 2. 你对自己 3 年内的职业规划是什么？
> 3. 你为什么会考虑我们公司 / 这个岗位？

**你的回答：**

---
