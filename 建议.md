## 数据组织层
职责：
对接服务端数据，数据加工和适配，承载前端业务逻辑，逻辑复用
这里我理解逻辑复用是编程提效的常用手段，但不是数据层最重要的东西，能复用就尽可能复用，这取决于设计和交互
最重要也是最基本的要求就是承载业务逻辑以及数据适配

### 数据流向：
api->数据层->UI层
数据层:
loader->基础数据->被加工的数据(分为通用和特用，这里需要拆分好)->UI
这里最关键的是在于数据的加工，我们需要维护各种适配器，数据通过适配器能转化成UI想要的数据，最终转化的数据结构和字段可以由数据层开发和UI层开发协商定义，这里需要UI层开发参与，甚至让UI层自己定义都行，提高工作效率

### 数据层相关问题:
一定要用类吗？可用可不用吧，个人觉得js并不像java那样面向对象编程，js里的类的实现原理还是基于原型链的关系委托，并非复制
如何输出数据？这里分为展示数据和不展示数据，所以不一定要用响应式，所以输出数据也不一定要用hook，甚至一个普通函数都行

目录结构划分？按功能或模块划分，可以参考后端如何划分？能保持一致尽量保持一致
一级目录千万不要按页面划分，没有意义，就把自己当成是一个可以输出响应式数据的后端就行。可以想象一个目录就是一个模块，里面包含数据请求、接收、改造适配、输出数据，其实就可以按照职责划分，比如loader、PO定义、适配器、数据输出函数、常量以及内部util等等

## UI层
页面交互的实现，以及内部状态管理，通过调用数据层，将整个功能串起来

国际化文案管理->不希望数据层代码出现t(xxxx)，集中在UI层统一管控

## 关于项目目录划分的两点建议：
对于前端来说，无非就是函数和组件，在现代MVVM框架中组件即函数，因此我们需要按照函数的功能职责去划分
1. 区分好无状态(傻瓜)组件和有状态(聪明)组件，按类别分开，可复用组件提升，不可复用组件跟着页面走  ---- 让它们各司其职，傻瓜组件负责展示和样式，聪明组件负责逻辑和数据输入,我们在实践的时候应该把数据注入逻辑提升到最近的聪明组件，严格遵守数据流自上而下的原则，通常目录划分可以是component放傻瓜组件，pages/views放聪明组件，不能复用的就自治吧----也就是page下有自己的components
2. 按照有无副作用划分函数，副作用函数可以是hook(对UI的副作用)或者其他异步函数(I/O处理、读取文件)。大部分同步函数可以保持无副作用，异步函数则天生自带副作用；通常可以把无副作用函数视为utils，有副作用函数划为services或者hooks
这样做的好处是，分类清晰职责分明，利于高效开发、易维护

## 其他建议：
我看到和感受到的最大的问题就是慢，打包慢，测试环境慢，代码扫描慢，在页面体验上来说，打开就是大把的请求，肉眼可见的慢。

看到的一个现象是前端存在一些遍历调接口的情况，也不管数组的长度，直接Promise.all一把梭，甚至都不管Promise.all的特性是一个失败都得失败，这很明显影响了交互体验, 所以这种情况其实能杜绝就尽量杜绝，能聚合接口就聚合下，不说tcp连接耗时，http一个来回也是资源的消耗啊。

需要并发多个请求场景需要控制并发:
浏览器虽然会限制并发数量，但http3之前的队头阻塞问题一致没得到很好解决，任务一多就会卡，会乱。
因此我们需要做并发控制，加上并发处理函数去处理多个请求并发场景，集成并发控制、失败重试、异常处理等能力